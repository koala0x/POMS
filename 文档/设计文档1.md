# design.md

```markdown
# 加密叙事雷达系统 - 设计文档

> **项目定位**:不是"新闻摘要器",而是"加密叙事雷达"。
> **核心使命**:从海量社交数据中,**最快发现正在形成的新共识**。

---

## 目录

- [1. 项目背景](#1-项目背景)
- [2. 设计目标与非目标](#2-设计目标与非目标)
- [3. 核心理念](#3-核心理念)
- [4. 系统架构](#4-系统架构)
- [5. 数据流详解](#5-数据流详解)
- [6. 数据库设计](#6-数据库设计)
- [7. 模块设计](#7-模块设计)
- [8. 关键算法](#8-关键算法)
- [9. 技术栈](#9-技术栈)
- [10. 硬件配置](#10-硬件配置)
- [11. 落地路径](#11-落地路径)
- [12. 风险与权衡](#12-风险与权衡)
- [13. 后续演进](#13-后续演进)

---

## 1. 项目背景

### 1.1 现状

已具备稳定的多源数据采集能力:
- Twitter / X
- Discord
- 币安广场
- (未来扩展:Telegram、Reddit、链上数据)

### 1.2 现有方案的问题

当前 pipeline 为:
```
Python 预处理 → LLM 一次摘要 → LLM 二次摘要
```

存在三个结构性缺陷:

1. **批次边界是时间切的,不是话题切的**
   - 20 条推文涉及 15 个项目,模型只能流水账总结
2. **缺乏跨条目去重**
   - 加密社交数据 30-50% 是转推/复读,LLM 反复读相同内容
3. **缺失"实体维度"**
   - 热点的本质是"某实体在时间窗内提及频率/速度异常",LLM 不擅长这种统计任务

**核心结论**:LLM 擅长"浓缩文本",不擅长"从 10 万条里统计谁最火"。后者本质是**信息检索+聚合**,该交给代码。

---

## 2. 设计目标与非目标

### 2.1 目标(Goals)

- ✅ 从持续流入的多源数据中,**自动发现近期热点**
- ✅ 识别"**正在形成的新叙事**"(而非已被广泛讨论的旧热点)
- ✅ 输出**结构化的热点简报**(叙事、催化剂、龙头、情绪、风险)
- ✅ 支持**实时警报**(增长率突变时立即推送)
- ✅ 在**96G 内存、纯 CPU**主机上稳定运行
- ✅ 复用现有工程骨架(SQLAlchemy / scheduler / Ollama / prefilter)

### 2.2 非目标(Non-Goals)

- ❌ 不做交易决策、不做喊单
- ❌ 不追求覆盖所有信息(覆盖率 vs 信噪比,选信噪比)
- ❌ 不做实时秒级响应(分钟级即可)
- ❌ 不在第一阶段做链上数据接入

---

## 3. 核心理念

### 3.1 三句话原则

1. **"热点 = 增长率,不是绝对值"**
   - BTC 永远高频,但真正的 alpha 是从冷到热的爆发
2. **"LLM 只在最后一公里介入"**
   - 统计/检索的活让代码干,LLM 只做解释和总结
3. **"做雷达,不做百科"**
   - 找新叙事 > 总结全市场

### 3.2 反模式(避免)

| 反模式 | 为什么错 |
|--------|---------|
| 把全部消息丢给大模型总结 | Token 爆炸,且 LLM 倾向"平均化",会忽略小而急的新热点 |
| 用 LLM 做去重 | 浪费算力,SimHash/Embedding 毫秒级搞定 |
| 只看绝对提及量 | BTC/ETH 永远第一,看不到新叙事 |
| 单源判断热点 | 容易被单平台噪声/水军误导 |

---

## 4. 系统架构

### 4.1 六层架构图

```
┌──────────────────────────────────────────────────────┐
│  数据源:Twitter / Discord / 币安广场 / ...          │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L0  归一化 + 近似去重                                │
│      纯代码 / SimHash / Embedding                     │
│      产出:去重后的标准化消息                          │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L1  实体抽取 + 关键词提取                            │
│      正则 + 词典 + 小模型兜底                          │
│      产出:每条消息挂 0-N 个实体标签                  │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L2  热度统计层 ⭐ 系统核心                            │
│      纯数学:增长率 / 共现 / 跨源加权 / 情绪          │
│      产出:Top-K 热点实体排行                          │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L3  Embedding 聚类层                                 │
│      bge-m3 + HDBSCAN                                  │
│      产出:话题簇(说同一件事的消息归为一簇)        │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L4  LLM 定向简报                                     │
│      Qwen3 8B/14B,固定 JSON 模板                      │
│      产出:每个热点话题的结构化简报                   │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  L5  产出层                                           │
│      每日雷达报告 + 实时警报 + Streamlit 面板         │
└──────────────────────────────────────────────────────┘
```

### 4.2 LLM 介入位置

整个 pipeline 中,**LLM 只在两处介入**:
- **L1 兜底**:对未命中词典的消息做实体抽取(可选,batch处理)
- **L4 简报**:对 Top-K 话题簇做结构化总结(主力)

这是控制成本和质量的关键。

---

## 5. 数据流详解

### 5.1 一条消息的完整生命周期

```
[原始消息]
  "Just aped into $LAUNCHCOIN, AI Agent narrative on Base is heating up 🚀"
  source=twitter, author=@xxx, ts=2025-01-15T10:23:00, likes=152

  ↓ L0 归一化
[标准化消息]
  text, author, ts, source, engagement=152

  ↓ L0 去重(SimHash / Embedding)
[去重通过] (假设非重复)

  ↓ L1 实体抽取
[挂标签]
  entities = ["$LAUNCHCOIN", "AI Agent", "Base"]
  sentiment_words = ["aped", "heating up", "🚀"]

  ↓ 写入 entity_mentions 表
[等待统计]

  ↓ L2 时间窗聚合(每 15min 触发)
[计算]
  $LAUNCHCOIN 1h 提及 = 900,7天均值/h = 20
  growth = 45x → 触发警报

  ↓ L3 聚类(对该实体的消息池)
[话题簇]
  Cluster_A: 关于 $LAUNCHCOIN 的 230 条消息
  代表消息(簇心附近) × 5

  ↓ L4 LLM 简报
[结构化输出]
  {
    "narrative": "新 AI Agent 项目在 Base 上爆发",
    "catalyst": "项目方公布 V2 路线图 + 大V 喊单",
    "key_players": ["LAUNCHCOIN", "Virtuals"],
    "sentiment": "bullish",
    "stage": "爆发期",
    ...
  }

  ↓ L5 产出
[写入日报 + 触发实时警报]
```

### 5.2 调度时序

| 任务 | 频率 | 说明 |
|------|------|------|
| L0+L1 处理新消息 | 持续(消费队列) | 来一条处理一条 |
| L2 热度统计 | 每 15 分钟 | 滑动窗口聚合 |
| L3 聚类 + L4 简报 | 每 1-2 小时 | 对当前 Top-K 跑 |
| L5 日报 | 每日 1 次 | 整合当日热点 |
| 实时警报 | 增长率 >10x 即触发 | 不等定时任务 |

---

## 6. 数据库设计

### 6.1 复用现有表

- `raw_messages`:原始消息(已存在)
- 现有 prefilter 输出表

### 6.2 新增表

```sql
-- 标准化 + 去重后的消息(L0 产出)
CREATE TABLE normalized_messages (
    id              BIGSERIAL PRIMARY KEY,
    raw_id          BIGINT REFERENCES raw_messages(id),
    text            TEXT NOT NULL,
    author          VARCHAR(128),
    source          VARCHAR(32) NOT NULL,  -- twitter/discord/binance
    ts              TIMESTAMP NOT NULL,
    engagement      INTEGER DEFAULT 0,     -- 点赞+转发+回复
    simhash         BIGINT,                -- 去重用
    embedding       VECTOR(1024),          -- 可选,bge-m3 维度
    is_duplicate    BOOLEAN DEFAULT FALSE,
    dup_of          BIGINT,                -- 指向"原版"id
    created_at      TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_norm_ts ON normalized_messages(ts);
CREATE INDEX idx_norm_source ON normalized_messages(source);
CREATE INDEX idx_norm_simhash ON normalized_messages(simhash);

-- 实体提及记录(L1 产出,L2 消费)
CREATE TABLE entity_mentions (
    id              BIGSERIAL PRIMARY KEY,
    msg_id          BIGINT REFERENCES normalized_messages(id),
    entity          VARCHAR(128) NOT NULL,    -- $BTC / Base / AI Agent
    entity_type     VARCHAR(32),              -- ticker/chain/narrative/project/kol
    source          VARCHAR(32) NOT NULL,
    ts              TIMESTAMP NOT NULL,
    engagement      INTEGER DEFAULT 0,
    confidence      FLOAT DEFAULT 1.0         -- 词典=1.0, 模型抽取=0.6-0.9
);
CREATE INDEX idx_em_entity_ts ON entity_mentions(entity, ts);
CREATE INDEX idx_em_ts ON entity_mentions(ts);

-- 热度快照(L2 产出)
CREATE TABLE hotness_snapshots (
    id              BIGSERIAL PRIMARY KEY,
    window_start    TIMESTAMP NOT NULL,
    window_end      TIMESTAMP NOT NULL,
    entity          VARCHAR(128) NOT NULL,
    count_short     INTEGER,           -- 短窗口提及数
    count_baseline  FLOAT,             -- 长窗口/h 均值
    growth_rate     FLOAT,             -- 增长率
    cross_source    INTEGER,           -- 出现在几个源
    engagement_sum  INTEGER,
    final_score     FLOAT,             -- 综合分
    rank            INTEGER,
    created_at      TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_hot_window ON hotness_snapshots(window_end, rank);

-- 共现关系(L2 产出,用于新叙事检测)
CREATE TABLE entity_cooccurrence (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    entity_a        VARCHAR(128),
    entity_b        VARCHAR(128),
    cooccur_count   INTEGER,
    growth_rate     FLOAT,
    UNIQUE(window_end, entity_a, entity_b)
);

-- 话题簇(L3 产出)
CREATE TABLE topic_clusters (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    cluster_label   VARCHAR(256),
    entities        TEXT[],            -- 簇关联的实体
    msg_ids         BIGINT[],          -- 簇内消息id
    centroid        VECTOR(1024),
    size            INTEGER,
    score           FLOAT,
    created_at      TIMESTAMP DEFAULT NOW()
);

-- 叙事简报(L4 产出)
CREATE TABLE narrative_briefs (
    id              BIGSERIAL PRIMARY KEY,
    cluster_id      BIGINT REFERENCES topic_clusters(id),
    window_end      TIMESTAMP NOT NULL,
    narrative       TEXT,
    catalyst        TEXT,
    key_players     TEXT[],
    sentiment       VARCHAR(16),       -- bullish/bearish/neutral
    fund_logic      TEXT,
    risk            TEXT,
    stage           VARCHAR(16),       -- 萌芽/爆发/高潮/衰退
    sample_msg_ids  BIGINT[],
    raw_llm_output  JSONB,
    created_at      TIMESTAMP DEFAULT NOW()
);

-- 警报记录
CREATE TABLE alerts (
    id              BIGSERIAL PRIMARY KEY,
    entity          VARCHAR(128),
    trigger_type    VARCHAR(32),       -- growth_surge/new_narrative/cross_platform
    score           FLOAT,
    payload         JSONB,
    sent_at         TIMESTAMP,
    created_at      TIMESTAMP DEFAULT NOW()
);
```

---

## 7. 模块设计

### 7.1 目录结构(基于现有代码扩展)

```
project/
├── db/
│   ├── connection.py          [保留]
│   └── models.py              [扩展:新表]
├── scheduler/
│   └── jobs.py                [扩展:新job]
├── llm/
│   └── ollama_client.py       [保留]
├── prompts/
│   ├── entity_extract.txt     [新增]
│   └── narrative_brief.txt    [新增]
├── services/
│   ├── prefilter.py           [保留,小幅改造]
│   ├── normalizer.py          [新增] L0
│   ├── dedup.py               [新增] L0
│   ├── embedding.py           [新增] L0/L3 共用
│   ├── entity_extractor.py    [新增] L1
│   ├── hotness.py             [新增] L2
│   ├── cooccurrence.py        [新增] L2
│   ├── clustering.py          [新增] L3
│   ├── briefing.py            [新增] L4 (改造原 level1_service)
│   ├── daily_report.py        [新增] L5 (改造原 level2_service)
│   └── alerting.py            [新增] L5
├── dictionaries/
│   ├── tickers.yaml           [新增] 代币词典
│   ├── chains.yaml            [新增] 链词典
│   ├── narratives.yaml        [新增] 叙事词典
│   └── kols.yaml              [新增] KOL词典
└── ui/
    └── streamlit_app.py       [新增] 可视化面板
```

### 7.2 核心模块接口

#### `services/dedup.py`

```python
class Deduplicator:
    def __init__(self, mode: str = "simhash"):
        """mode: 'simhash' | 'embedding'"""

    def is_duplicate(self, text: str) -> tuple[bool, Optional[int]]:
        """返回 (是否重复, 原版msg_id)"""

    def add(self, msg_id: int, text: str, embedding: Optional[list] = None):
        """加入索引"""
```

#### `services/entity_extractor.py`

```python
class EntityExtractor:
    def __init__(self, dict_path: str):
        """加载词典"""

    def extract(self, text: str) -> list[Entity]:
        """
        三层抽取:
        1. 正则:$TICKER, 0x..., @handle
        2. 词典匹配
        3. (可选)小模型兜底
        """

@dataclass
class Entity:
    name: str
    type: str           # ticker/chain/narrative/project/kol
    confidence: float
```

#### `services/hotness.py`

```python
class HotnessCalculator:
    def compute_window(
        self,
        window_end: datetime,
        short_hours: int = 1,
        baseline_days: int = 7,
    ) -> list[HotnessRecord]:
        """计算指定窗口的热度排行"""

    def get_top_k(self, window_end: datetime, k: int = 20) -> list[HotnessRecord]:
        """读取 Top-K"""
```

#### `services/clustering.py`

```python
class TopicClusterer:
    def cluster(
        self,
        msg_ids: list[int],
        embeddings: np.ndarray,
        min_cluster_size: int = 5,
    ) -> list[TopicCluster]:
        """HDBSCAN 聚类"""
```

#### `services/briefing.py`

```python
class BriefingService:
    async def generate_brief(
        self,
        cluster: TopicCluster,
        context: HotnessRecord,
    ) -> NarrativeBrief:
        """对单个簇生成结构化简报"""
```

---

## 8. 关键算法

### 8.1 增长率打分(L2 核心)

```python
def compute_growth_rate(
    entity: str,
    window_end: datetime,
    short_hours: int = 1,
    baseline_days: int = 7,
) -> float:
    short_count = count_mentions(entity, window_end - short_hours, window_end)

    baseline_total = count_mentions(
        entity,
        window_end - baseline_days * 24,
        window_end - short_hours,
    )
    baseline_per_hour = baseline_total / (baseline_days * 24 - short_hours)

    # 平滑因子防除零和冷启动
    SMOOTHING = 2.0
    growth = short_count / max(baseline_per_hour, SMOOTHING)
    return growth
```

### 8.2 综合热度分

```python
def final_score(record: HotnessRecord) -> float:
    growth   = record.growth_rate                        # 主信号
    cross    = 1 + 0.3 * (record.cross_source - 1)      # 跨源加权
    eng      = math.log1p(record.engagement_sum)        # 互动加权
    return growth * cross * eng
```

### 8.3 共现网络(新叙事检测)

```python
def detect_new_narrative(window_end: datetime, top_k: int = 20):
    """
    思路:
    - 取 Top-K 热点实体
    - 两两组合,统计共现频次
    - 对比基线期共现频次
    - 共现增长率 > 阈值 → 新叙事候选
    """
    hot_entities = get_top_k(window_end, top_k)
    new_pairs = []
    for a, b in combinations(hot_entities, 2):
        cur = cooccur_count(a, b, window_end - 1h, window_end)
        base = cooccur_count(a, b, window_end - 7d, window_end - 1h) / (7 * 24)
        if cur / max(base, 1) > 5.0:
            new_pairs.append((a, b, cur / max(base, 1)))
    return new_pairs
```

### 8.4 簇代表消息选择

```python
def select_representatives(cluster: TopicCluster, n: int = 5) -> list[int]:
    """
    多样性 + 互动量综合选择
    - 50% 取簇心最近的(代表性)
    - 50% 取互动量最高的(权威性)
    - 去除文本高度相似的
    """
```

---

## 9. 技术栈

| 类别 | 技术选型 | 备注 |
|------|---------|------|
| 数据库 | PostgreSQL | 后期可加 ClickHouse 做时序 |
| 向量库 | **FAISS**(本地文件) | 别上来就 Milvus,太重 |
| Embedding | `bge-m3` (via sentence-transformers) | 中英混合最佳 |
| 去重 | `datasketch` (SimHash) → bge-m3 | 二阶段升级 |
| NLP | regex / jieba / spaCy | 轻量 |
| 聚类 | **HDBSCAN** | 无需指定簇数 |
| LLM 框架 | **Ollama**(已有) | 复用 |
| 调度 | 现有 scheduler | 复用 |
| 可视化 | **Streamlit** | 快速搭面板 |
| 警报 | Telegram Bot / 企业微信 | 二期接入 |

---

## 10. 硬件配置

**主机配置**:96G 内存,纯 CPU(无 GPU 假设)

| 用途 | 模型 | 大小(Q4) | 频率 |
|------|------|---------|------|
| Embedding | `bge-m3` | ~2GB | 持续(L0+L3) |
| 实体抽取兜底 | `qwen3:8b` | ~5GB | 中频 |
| L4 简报 | `qwen3:8b` 或 `qwen3:14b` | 5-9GB | 每 1-2h |
| L5 总评(可选) | `qwen3:32b` | ~20GB | 每日 1 次 |

**关键原则**:
- CPU 推理瓶颈是 token/s,**省钱 = 让 LLM 少跑**
- 本架构 LLM 只处理 Top-K 簇,日调用量级在几十~几百次
- 内存绰绰有余,不会成为瓶颈

---

## 11. 落地路径

### Phase 1:止血(1 周)— 立刻见效

| Task | 模块 | 产出 |
|------|------|------|
| 1.1 | `services/normalizer.py` | 字段统一 |
| 1.2 | `services/dedup.py`(SimHash 起步) | LLM调用量砍 30-50% |
| 1.3 | `dictionaries/*.yaml` | 词典 v1 |
| 1.4 | `services/entity_extractor.py`(正则+词典) | 每条消息挂标签 |
| 1.5 | `services/hotness.py`(增长率) | **每日 Top 20 排行榜** ✅ |

> **里程碑**:即使没接 LLM 简报,你已经能每天看到"哪些词在爆"。这本身就是产品。

### Phase 2:升级(2 周)— 质量跃迁

| Task | 模块 | 产出 |
|------|------|------|
| 2.1 | `services