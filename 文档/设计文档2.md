# 加密叙事雷达系统 · 完整设计文档 v2.0

> **项目定位**：不是"新闻摘要器"，不是"行情播报机"，而是**加密叙事雷达**。
> **核心使命**：从海量社交噪音中，最快发现"正在从冷到热的新共识"——在别人看懂之前，你已经看到了。

---

## 目录

1. [项目背景与核心问题](#1-项目背景与核心问题)
2. [设计哲学](#2-设计哲学)
3. [系统总架构](#3-系统总架构)
4. [数据流全生命周期](#4-数据流全生命周期)
5. [各层详细设计](#5-各层详细设计)
   - [L0 归一化与去重](#l0-归一化与去重)
   - [L1 实体抽取](#l1-实体抽取)
   - [L2 热度统计与激增检测](#l2-热度统计与激增检测)
   - [L3 共现网络与叙事发现](#l3-共现网络与叙事发现)
   - [L4 Embedding聚类](#l4-embedding聚类)
   - [L5 LLM定向简报](#l5-llm定向简报)
   - [L6 产出与警报](#l6-产出与警报)
6. [关键算法设计](#6-关键算法设计)
7. [数据库完整设计](#7-数据库完整设计)
8. [模块目录与接口](#8-模块目录与接口)
9. [调度时序设计](#9-调度时序设计)
10. [技术栈选型](#10-技术栈选型)
11. [硬件资源规划](#11-硬件资源规划)
12. [落地路径（分阶段）](#12-落地路径分阶段)
13. [反模式清单](#13-反模式清单)
14. [后续演进路线](#14-后续演进路线)

---

## 1. 项目背景与核心问题

### 1.1 你已经有的

- 持续流入的多源数据：Twitter/X、Discord、币安广场
- 现有 pipeline：Python预处理 → LLM一次摘要 → LLM二次摘要
- 本地主机：96G内存，纯CPU
- 现有工程骨架：SQLAlchemy、scheduler、Ollama、prefilter

### 1.2 现有方案的三个结构性缺陷

**缺陷一：批次边界是时间切的，不是话题切的**

一批20条推文可能涉及15个不同项目，LLM只能流水账式地"这个说了A，那个说了B"。既不知道哪个是热点，也无法感知跨条目的共鸣。

**缺陷二：没有跨条目去重**

加密社交数据有30%~50%是转推、复读、同一条新闻的不同KOL转述。模型被迫反复读同一件事，还会误以为"重复多=重要"，这是复读机效应，不是真热点。

**缺陷三：LLM被用错了地方**

LLM擅长"把一段文字浓缩成摘要"，不擅长"从10万条里统计出谁最火"。后者本质是信息检索+时序聚合，该让代码干，而且代码干得更快、更准、成本为零。

**最关键的认知错误**：大模型在总结时会"平均化"——它会优先总结主流内容，而忽略那个只占3%但增长速度80倍的新词。但加密市场真正的alpha恰恰来自**正在形成的新共识**，而不是旧共识。

### 1.3 真正要解决的问题

```
不是：怎么总结更多内容
而是：怎么在噪音中最快发现"从冷到热的信号"
```

---

## 2. 设计哲学

### 2.1 三句话原则

**① 热点 = 增长率，不是绝对值**

BTC永远高频，但真正的alpha是从冷到热的爆发。`Launchcoin` 平均每小时20次提及，突然变成900次，这个45倍才是信号，不是BTC稳定的5000次。

**② LLM只在最后一公里介入**

统计的活让代码干，检索的活让向量干，LLM只做一件事：对已经被数学确认为热点的内容，用人话解释"为什么热、热在哪、怎么看"。

**③ 做雷达，不做百科**

雷达的价值在于发现**新出现的信号**，而不是描述已知的全貌。系统的核心竞争力是早，不是全。

### 2.2 信号层级（重要性从高到低）

| 信号类型 | 描述 | 实现层 |
|---------|------|-------|
| 增长率激增 | 某实体短窗口提及量 vs 长期基线的比值 | L2 |
| 多源共振 | 同一实体在Twitter + Discord + 广场同时热 | L2 |
| 共现新组合 | 两个词突然开始频繁一起出现 | L3 |
| 情绪转向 | 同一实体的情绪从中性/负面急转正面 | L2 |
| KOL首发 | 某词首次由高影响力账号提及 | L1+L2 |
| 新词出现 | 基线期从未见过，突然出现的词 | L2 |

---

## 3. 系统总架构

### 3.1 七层漏斗架构

```
┌─────────────────────────────────────────────────────────────────┐
│  数据源层                                                        │
│  Twitter/X  ·  Discord  ·  币安广场  ·  (未来: Telegram/Reddit) │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 原始消息流
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L0  归一化 + 近似去重                                           │
│  纯代码 · SimHash粗筛 → Embedding精筛                            │
│  ► 产出：去重后的标准化消息（砍掉30~50%重复）                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L1  实体抽取 + 关键词提取                                       │
│  正则抓$TICKER · 词典匹配 · 小模型兜底长尾                       │
│  ► 产出：每条消息挂 0~N 个实体标签 + 情绪词                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L2  热度统计层  ⭐ 系统核心                                      │
│  增长率 · 跨源加权 · 情绪强度 · 新词检测                         │
│  ► 产出：每15分钟 Top-K 热点实体排行 + 实时激增警报              │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L3  共现网络层  ⭐ 叙事发现核心                                  │
│  实体两两共现统计 · 共现增长率 · 新叙事组合识别                  │
│  ► 产出：正在形成的叙事组合（如 AI Agent + Base + Virtuals）     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L4  Embedding 聚类层                                            │
│  bge-m3向量化 · HDBSCAN聚类 · 簇代表消息提取                    │
│  ► 产出：话题簇（说同一件事的消息归为一簇）                     │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L5  LLM 定向简报层                                              │
│  Qwen3 8B/14B · 固定JSON模板 · 只处理Top-K簇                    │
│  ► 产出：每个热点的结构化简报（叙事/催化剂/龙头/情绪/风险/阶段）│
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L6  产出层                                                      │
│  每日雷达报告 · 实时激增警报 · Streamlit可视化面板               │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 LLM介入的精确位置

整个pipeline中，**LLM只在两处介入**：

- **L1 兜底**（可选）：对未命中词典的消息做实体抽取，batch处理，低频
- **L5 简报**（主力）：对已通过数学确认的Top-K话题簇做结构化总结

这是控制成本和推理质量的关键设计。LLM的日调用量控制在**几十到几百次**，不是几万次。

---

## 4. 数据流全生命周期

### 4.1 一条消息从进入到产出的完整旅程

```
[原始消息]
  "Just aped into $LAUNCHCOIN, AI Agent narrative on Base is 
   heating up, Virtuals ecosystem exploding 🚀🚀🚀"
  source=twitter · author=@kol_xxx · ts=2025-06-01T10:23:00 · likes=1520

  ↓ L0 归一化
  清洗emoji → 保留🚀作情绪信号
  统一字段: text / author / ts / source / engagement=1520

  ↓ L0 去重
  SimHash计算 → 与近24h消息对比 → 无重复，通过

  ↓ L1 实体抽取
  正则命中: $LAUNCHCOIN
  词典命中: [Base, Virtuals, AI Agent]
  情绪词:   [aped, heating up, exploding, 🚀×3]
  KOL标记:  @kol_xxx 粉丝量>5万 → 高权重

  ↓ 写入 entity_mentions 表（4条记录，对应4个实体）

  ↓ L2 热度统计（每15分钟触发）
  $LAUNCHCOIN: 1h提及=900, 7天均值/h=20 → growth=45x ⚡激增触发
  Base:        1h提及=340, 7天均值/h=280 → growth=1.2x 正常
  AI Agent:    1h提及=520, 7天均值/h=85  → growth=6.1x ⚡激增触发
  Virtuals:    1h提及=290, 7天均值/h=40  → growth=7.2x ⚡激增触发
  跨源检测: $LAUNCHCOIN 在Twitter+Discord同时热 → cross_source=2 加权

  ↓ L3 共现网络分析
  ($LAUNCHCOIN, AI Agent) 共现增长: 38x → 新叙事候选 ⭐
  (AI Agent, Virtuals, Base) 三角共现增长: 22x → 新叙事确认 ⭐

  ↓ L4 Embedding聚类
  对 $LAUNCHCOIN 的830条消息做bge-m3向量化
  HDBSCAN聚出3个簇:
    Cluster_A(620条): 主叙事，AI Agent+Base生态
    Cluster_B(140条): 价格讨论
    Cluster_C(70条): 技术讨论
  提取Cluster_A的5条代表消息

  ↓ L5 LLM简报（Qwen3:14b）
  输入: 5条代表消息 + 热度数据 + 共现关系
  输出:
  {
    "narrative": "新AI Agent基础设施项目在Base链上爆发，Virtuals生态作为龙头带动LAUNCHCOIN等新标的",
    "catalyst": "Virtuals V2路线图发布 + 头部KOL @kol_xxx 喊单，触发Base链AI Agent叙事共振",
    "key_players": ["$LAUNCHCOIN", "$VIRTUAL", "Base"],
    "sentiment": "极度看多",
    "fund_logic": "AI Agent赛道资金从ETH主网向Base迁移，Virtuals生态作为承接点，LAUNCHCOIN是其中弹性最高的新标的",
    "risk": "KOL驱动行情，无基本面支撑，注意砸盘风险",
    "stage": "爆发期",
    "time_to_act": "高"
  }

  ↓ L6 产出
  写入 narrative_briefs 表
  触发实时Telegram警报（增长率>10x）
  纳入当日雷达报告
```

---

## 5. 各层详细设计

### L0 归一化与去重

**目标**：把数据量砍掉30~50%，消除复读机效应。

#### 归一化字段标准

```python
@dataclass
class NormalizedMessage:
    text: str           # 清洗后的正文（保留$TICKER、表情情绪词）
    author: str         # 作者ID
    author_weight: float  # 基于粉丝量/历史影响力的权重，范围0.1~3.0
    ts: datetime        # 发布时间（统一UTC）
    source: str         # twitter / discord / binance_square
    engagement: int     # 点赞+转发+回复（归一化处理，各平台口径不同）
    raw_id: int         # 关联原始消息ID
```

#### 清洗规则

- 移除纯链接帖、纯表情帖、少于5个有效词的帖子
- 保留表情作为情绪信号（🚀=看多，💀=看空，🤔=质疑）
- 清洗广告账号特征（低粉丝+高发帖频率+固定模板）
- 统一$TICKER大写

#### 两阶段去重

**第一阶段（SimHash，极快）**

- 对文本计算SimHash（64位）
- 汉明距离≤3 → 判为重复
- 内存维护过去24小时的SimHash集合
- 适合：转发变体、轻微改词的复读

**第二阶段（Embedding，精准，可选升级）**

- 使用bge-m3生成向量
- cosine相似度≥0.92 → 判为重复
- 适合：同一消息的中英互译、不同措辞转述
- 建议：数据量稳定后升级到此方案

**去重策略**：同一内容保留互动量最高的那条，记录`dup_of`关联。

---

### L1 实体抽取

**目标**：让每条消息带上"说的是谁/什么"的标签，这是后面所有统计的基础。

#### 三层抽取策略

**第一层：正则（毫秒级，必做）**

```
$TICKER 模式:    \$[A-Z]{1,10}\b
合约地址(EVM):   0x[a-fA-F0-9]{40}
合约地址(Solana): [1-9A-HJ-NP-Za-km-z]{32,44}
Twitter handle:  @[a-zA-Z0-9_]{1,15}
```

**第二层：词典匹配（毫秒级，必做）**

维护以下词典（YAML格式，持续更新）：

```yaml
# tickers.yaml 示例
BTC:
  aliases: [比特币, bitcoin, 大饼, 老大]
  type: layer1
ETH:
  aliases: [以太坊, ethereum, 以太, 大饼二]
  type: layer1
HYPE:
  aliases: [hyperliquid, HL]
  type: defi
# ...按需扩展，重点维护你关注的长尾项目

# narratives.yaml 示例
AI_Agent:
  keywords: [AI agent, autonomous agent, agent protocol, ai16z]
  type: narrative
RWA:
  keywords: [real world asset, tokenized, RWA]
  type: narrative
```

词典支持：代币名/链名/叙事/KOL/交易所，命中confidence=1.0。

**第三层：小模型兜底（分钟级，选做）**

对未命中任何词典的消息，积攒成batch（50条/次），送给`qwen3:8b`做结构化抽取：

```
Prompt: 从以下加密货币社区消息中，提取所有提到的：
代币名、项目名、区块链名、协议名、KOL名称。
仅输出JSON，格式：{"entities": [{"name":"xxx","type":"ticker/chain/project/kol"}]}
不要解释，不要其他内容。
```

命中confidence=0.6~0.85（根据模型置信度）。

#### 情绪词提取

同步提取情绪信号：

```python
BULLISH_SIGNALS = {
    'strong': ['ape', 'exploding', 'moon', '100x', '🚀🚀🚀', 'send it'],
    'mild':   ['bullish', 'heating up', 'watching', 'accumulate']
}
BEARISH_SIGNALS = {
    'strong': ['rug', 'exit scam', 'dump', 'rekt', '💀'],
    'mild':   ['bearish', 'careful', 'risky', 'overbought']
}
```

---

### L2 热度统计与激增检测

**这是整个系统最核心的层。** 它决定了你能看到什么，以及能多快看到。

#### 核心指标：增长率

```python
def compute_growth_rate(entity, window_end, short_hours=1, baseline_days=7):
    # 短窗口：最近N小时的提及次数
    short_count = count_mentions(entity, window_end - short_hours, window_end)
    
    # 基线：过去7天（排除最近short_hours）每小时平均值
    baseline_total = count_mentions(
        entity,
        window_end - timedelta(days=baseline_days),
        window_end - timedelta(hours=short_hours)
    )
    baseline_per_hour = baseline_total / (baseline_days * 24 - short_hours)
    
    # 平滑因子：防止冷启动时除以接近零的数
    # SMOOTHING=2 意味着：一个新词突然出现2次，增长率=1x，不触发警报
    # 只有出现足够多次才有意义
    SMOOTHING = 2.0
    return short_count / max(baseline_per_hour, SMOOTHING)
```

**多时间窗口并行**（不同粒度，捕获不同周期的热点）：

| 窗口配置 | 用途 |
|---------|------|
| 1h vs 7天均值 | 发现突发热点（主力窗口） |
| 6h vs 30天均值 | 发现中期上升趋势 |
| 15min vs 24h均值 | 发现超短期脉冲（配合实时警报） |

#### 综合热度评分

```python
def final_score(record):
    growth   = record.growth_rate                       # 主信号，权重最高
    cross    = 1 + 0.3 * (record.cross_source - 1)     # 跨源加权：出现在2个源+30%，3个源+60%
    eng      = math.log1p(record.engagement_sum)        # 互动量对数加权（防止单条爆款垄断）
    kol      = 1 + 0.5 * record.kol_mention_flag        # KOL首发加权+50%
    novelty  = 2.0 if record.is_new_entity else 1.0     # 新词额外加权×2
    
    return growth * cross * eng * kol * novelty
```

#### 特殊信号：新词检测

```python
def detect_new_entities(window_end):
    """
    基线期(7天前)从未出现过，最近1小时突然出现≥5次的词
    这类词增长率理论上无穷大，需要单独处理
    """
    recent = get_entities_in_window(window_end - 1h, window_end)
    historical = get_entities_in_window(window_end - 7d, window_end - 1h)
    new = [e for e in recent if e not in historical and recent[e] >= 5]
    return new  # 直接列为高优先级候选，bypass增长率计算
```

#### 实时激增触发器

不等定时任务，实时检测：

```python
ALERT_THRESHOLDS = {
    'critical': 20,   # 增长率>20x → 立即推送
    'high':     10,   # 增长率>10x → 纳入下次简报
    'medium':    5,   # 增长率>5x  → 纳入定期排行
}

# 每条消息处理完后触发检查（仅对新高频实体）
def on_new_mention(entity, current_count):
    if current_count % 50 == 0:  # 每累积50次提及检查一次，避免过度计算
        rate = compute_growth_rate(entity, now(), short_hours=0.25)  # 15分钟窗口
        if rate >= ALERT_THRESHOLDS['critical']:
            trigger_immediate_alert(entity, rate)
```

---

### L3 共现网络与叙事发现

**这是方案三独有的洞察，也是系统最有远见的设计。**

单个词的增长率告诉你"谁热"，共现网络告诉你"新叙事在形成"。

#### 共现统计

```python
def update_cooccurrence(message):
    """
    一条消息中，所有实体两两组合，记为共现一次
    例如消息含[LAUNCHCOIN, AI Agent, Base, Virtuals]
    → 产生6对共现: (LC,AI), (LC,Base), (LC,Virt), (AI,Base), (AI,Virt), (Base,Virt)
    """
    entities = message.entities
    for a, b in combinations(sorted(entities), 2):
        increment_cooccurrence(a, b, message.ts, message.source)
```

#### 新叙事检测

```python
def detect_emerging_narratives(window_end):
    """
    找出：最近1小时共现频次 vs 7天基线均值 增长率最高的实体对
    这些高增长共现对，就是正在形成的新叙事
    """
    hot_entities = get_top_k_entities(window_end, k=30)
    emerging = []
    
    for a, b in combinations(hot_entities, 2):
        cur = cooccur_count(a, b, window_end - 1h, window_end)
        base = cooccur_count(a, b, window_end - 7d, window_end - 1h) / (7 * 24)
        
        if cur >= 5 and cur / max(base, 0.5) > 5.0:
            emerging.append({
                'pair': (a, b),
                'growth': cur / max(base, 0.5),
                'count': cur
            })
    
    # 进一步寻找三角共现（三个词同时出现）→ 叙事更成熟
    triangles = find_triangle_cooccurrence(hot_entities, window_end)
    
    return sorted(emerging, key=lambda x: x['growth'], reverse=True)
```

#### 叙事生命周期追踪

```python
NARRATIVE_STAGES = {
    'emerging':  lambda r: r < 5x  and r > 2x,  # 萌芽期：增长但不明显
    'breakout':  lambda r: r >= 5x and r < 15x,  # 爆发期：核心入场窗口
    'peak':      lambda r: r >= 15x,             # 高潮期：谨慎追高
    'declining': lambda r: r < 2x  and prev > 5x # 衰退期：考虑离场
}
```

---

### L4 Embedding聚类

**目标**：把"说同一件事"的消息归为一簇，每簇提取5条代表性消息送给LLM，而不是把800条全给它。

#### 模型选择

推荐 `bge-m3`（BAAI/bge-m3）：
- 支持中英文混合（加密社区必须）
- 向量维度1024，质量优秀
- 内存占用约2GB，CPU推理可接受
- 输入长度支持8192 token

#### 聚类方案：HDBSCAN

为什么选HDBSCAN而不是K-Means或DBSCAN：
- **不需要预设簇数**：你不知道今天会有几个热点话题
- **能识别噪声点**：标记为-1的消息说明它是孤立的，不属于任何主题
- **对簇大小差异不敏感**：大话题几百条，小话题几条，都能正确识别

```python
from hdbscan import HDBSCAN

clusterer = HDBSCAN(
    min_cluster_size=5,      # 至少5条消息才构成一个话题
    min_samples=3,           # 核心点要求，影响噪声敏感度
    metric='euclidean',      # 配合L2归一化的向量
    cluster_selection_method='eom'  # Excess of Mass，适合大小不均的簇
)
```

#### 代表消息选择

```python
def select_representatives(cluster_embeddings, cluster_msg_ids, engagements, n=5):
    """
    多样性 + 权威性 综合选择
    - 50%：距簇心最近的消息（最能代表这个话题的中心观点）
    - 50%：互动量最高的消息（社区最认可/传播最广的声音）
    - 最终去除相互间cosine>0.95的重复（防止选出几乎一样的消息）
    """
    centroid = cluster_embeddings.mean(axis=0)
    
    # 按距离排序（代表性）
    distances = cosine_distances([centroid], cluster_embeddings)[0]
    by_distance = sorted(zip(distances, cluster_msg_ids))[:n//2 + 1]
    
    # 按互动量排序（权威性）
    by_engagement = sorted(zip(engagements, cluster_msg_ids), reverse=True)[:n//2 + 1]
    
    candidates = list(set([m for _, m in by_distance] + [m for _, m in by_engagement]))
    return deduplicate_by_similarity(candidates, threshold=0.95)[:n]
```

---

### L5 LLM定向简报

**目标**：用最少的token，生成最有价值的分析。

#### 推荐模型配置

| 用途 | 模型 | 触发频率 |
|------|------|---------|
| 热点简报（主力） | qwen3:14b | 每1~2小时，处理Top20话题 |
| 实时紧急简报 | qwen3:8b | 增长率>20x立即触发 |
| 日度总评 | qwen3:32b Q4 | 每天1次，整合全日热点 |

#### Prompt模板（固定结构化输出）

```
系统角色：你是一个专注于加密货币市场的叙事分析师，擅长识别市场正在形成的新共识。

以下是关于【{entity}】的热点数据：

[量化信号]
- 最近1小时提及次数：{short_count}次
- 过去7天每小时均值：{baseline}/h  
- 增长倍数：{growth_rate}x
- 跨平台分布：{sources}
- 综合热度分：{score}

[共现叙事]
{entity}最近频繁与以下概念同时出现：{cooccurrence_pairs}
（括号内为共现增长率）

[代表性消息（按多样性+影响力筛选）]
{sample_messages}

[任务]
请输出严格的JSON，不要任何额外文字：
{
  "narrative": "一句话说清楚这个热点的核心叙事（≤50字）",
  "catalyst": "触发这波热度的具体催化剂是什么（事件/消息/人）",
  "key_players": ["龙头项目或代币，最多3个"],
  "sentiment": "极度看多/看多/中性/看空/极度看空",
  "fund_logic": "资金关注这个叙事的底层逻辑是什么（≤80字）",
  "risk": "主要风险点（≤50字）",
  "stage": "萌芽期/爆发期/高潮期/衰退期",
  "confidence": 0.0-1.0,
  "time_sensitivity": "高/中/低（这个信号有多紧迫）"
}
```

#### 日度总评Prompt（L6使用qwen3:32b）

```
以下是今日加密市场的热点简报汇总（共{n}个话题）：
{all_briefs}

请进行主题聚合分析，输出JSON：
{
  "dominant_narratives": ["今日市场的主导叙事，按重要性排序"],
  "emerging_signals": ["值得明日重点关注的早期信号"],
  "sentiment_overview": "整体市场情绪评估",
  "narrative_rotation": "资金在哪些叙事之间轮动",
  "key_risks": ["当前最值得警惕的风险"],
  "tomorrow_watchlist": ["明日重点观察的实体"]
}
```

---

### L6 产出与警报

#### 输出形式

**① 实时激增警报**（Telegram Bot / 企业微信）

```
🚨 叙事激增警报

实体：$LAUNCHCOIN
增长率：45x（过去1小时900次提及 vs 均值20次/h）
跨平台：Twitter + Discord 同时热
共现叙事：AI Agent + Base + Virtuals

⚡ 阶段研判：爆发期
📊 时效性：高

[查看完整简报]
```

**② 每2小时热点简报**（Markdown文件）

结构：当前时段Top10热点 + 每个热点的结构化简报

**③ 每日雷达报告**（Markdown文件）

结构：主导叙事 + 叙事轮动 + 明日观察清单 + 风险提示

**④ Streamlit可视化面板**

- 实时热度排行榜（柱状图，自动刷新）
- 共现网络图（力导向图，展示叙事关联）
- 热点生命周期时间轴
- 多源情绪对比

---

## 6. 关键算法设计

### 6.1 SimHash去重（快速版）

```python
import hashlib

def simhash(text, bits=64):
    """
    SimHash: 对文本分词后，每个词的hash对总向量投票
    相似文本 → 相似SimHash → 汉明距离小
    """
    tokens = tokenize(text)  # jieba分词
    v = [0] * bits
    
    for token in tokens:
        h = int(hashlib.md5(token.encode()).hexdigest(), 16)
        for i in range(bits):
            if h & (1 << i):
                v[i] += 1
            else:
                v[i] -= 1
    
    fingerprint = 0
    for i in range(bits):
        if v[i] > 0:
            fingerprint |= (1 << i)
    return fingerprint

def hamming_distance(h1, h2):
    return bin(h1 ^ h2).count('1')

# 汉明距离≤3 → 相似（可调整，越小越严格）
def is_duplicate(h1, h2, threshold=3):
    return hamming_distance(h1, h2) <= threshold
```

### 6.2 滑动窗口计数（高效版）

```python
from collections import defaultdict, deque
import time

class SlidingWindowCounter:
    """
    纯内存实现，重启后重建（可接受）
    对于高频统计，比每次查数据库快100倍
    """
    def __init__(self):
        self.windows = {
            '15min': defaultdict(deque),
            '1h':    defaultdict(deque),
            '6h':    defaultdict(deque),
            '24h':   defaultdict(deque),
            '7d':    defaultdict(deque),
        }
        self.window_seconds = {
            '15min': 900,
            '1h':    3600,
            '6h':    21600,
            '24h':   86400,
            '7d':    604800,
        }
    
    def add(self, entity, ts=None):
        ts = ts or time.time()
        for window_name, deq in self.windows.items():
            deq[entity].append(ts)
    
    def count(self, entity, window='1h'):
        now = time.time()
        cutoff = now - self.window_seconds[window]
        deq = self.windows[window][entity]
        
        # 清理过期数据
        while deq and deq[0] < cutoff:
            deq.popleft()
        
        return len(deq)
    
    def growth_rate(self, entity, short='1h', baseline='7d'):
        short_count = self.count(entity, short)
        baseline_total = self.count(entity, baseline)
        baseline_hours = self.window_seconds[baseline] / 3600
        baseline_per_hour = baseline_total / baseline_hours
        
        return short_count / max(baseline_per_hour, 2.0)
```

### 6.3 共现矩阵（高效版）

```python
from itertools import combinations
from collections import defaultdict

class CooccurrenceTracker:
    def __init__(self):
        # key: frozenset({a, b}), value: SlidingWindowCounter
        self.counters = defaultdict(SlidingWindowCounter)
    
    def update(self, entities, ts=None):
        if len(entities) < 2:
            return
        for a, b in combinations(sorted(entities), 2):
            key = frozenset({a, b})
            self.counters[key].add('count', ts)
    
    def get_cooccurrence_growth(self, a, b):
        key = frozenset({a, b})
        counter = self.counters.get(key)
        if not counter:
            return 0
        return counter.growth_rate('count', short='1h', baseline='7d')
    
    def get_top_emerging_pairs(self, hot_entities, top_n=20):
        results = []
        for a, b in combinations(hot_entities, 2):
            growth = self.get_cooccurrence_growth(a, b)
            if growth > 3.0:
                results.append(((a, b), growth))
        return sorted(results, key=lambda x: x[1], reverse=True)[:top_n]
```

---

## 7. 数据库完整设计

推荐使用 **PostgreSQL**（后期可加ClickHouse做时序加速）。

如果初期想简化，SQLite也能支撑，日均百万条以内无压力，后期再迁移。

```sql
-- ============================================================
-- L0 产出：标准化消息
-- ============================================================
CREATE TABLE normalized_messages (
    id              BIGSERIAL PRIMARY KEY,
    raw_id          BIGINT,                    -- 关联原始消息
    text            TEXT NOT NULL,
    author          VARCHAR(128),
    author_weight   FLOAT DEFAULT 1.0,         -- KOL权重
    source          VARCHAR(32) NOT NULL,       -- twitter/discord/binance_square
    ts              TIMESTAMP NOT NULL,
    engagement      INTEGER DEFAULT 0,
    simhash         BIGINT,
    embedding       FLOAT[],                   -- bge-m3向量（可选，后期加pgvector）
    is_duplicate    BOOLEAN DEFAULT FALSE,
    dup_of          BIGINT,
    created_at      TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_norm_ts       ON normalized_messages(ts DESC);
CREATE INDEX idx_norm_source   ON normalized_messages(source, ts DESC);
CREATE INDEX idx_norm_simhash  ON normalized_messages(simhash);
CREATE INDEX idx_norm_dup      ON normalized_messages(is_duplicate) WHERE is_duplicate = FALSE;

-- ============================================================
-- L1 产出：实体提及记录
-- ============================================================
CREATE TABLE entity_mentions (
    id              BIGSERIAL PRIMARY KEY,
    msg_id          BIGINT REFERENCES normalized_messages(id),
    entity          VARCHAR(128) NOT NULL,
    entity_type     VARCHAR(32),               -- ticker/chain/narrative/project/kol
    source          VARCHAR(32) NOT NULL,
    ts              TIMESTAMP NOT NULL,
    engagement      INTEGER DEFAULT 0,
    author_weight   FLOAT DEFAULT 1.0,
    confidence      FLOAT DEFAULT 1.0,         -- 词典=1.0, 正则=0.95, 模型=0.6~0.9
    sentiment_score FLOAT DEFAULT 0,           -- -1(极度看空) ~ +1(极度看多)
    is_kol_mention  BOOLEAN DEFAULT FALSE
);
CREATE INDEX idx_em_entity_ts  ON entity_mentions(entity, ts DESC);
CREATE INDEX idx_em_ts         ON entity_mentions(ts DESC);
CREATE INDEX idx_em_source_ts  ON entity_mentions(source, ts DESC);

-- ============================================================
-- L2 产出：热度快照
-- ============================================================
CREATE TABLE hotness_snapshots (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    window_type     VARCHAR(16) NOT NULL,      -- '1h', '6h', '24h'
    entity          VARCHAR(128) NOT NULL,
    entity_type     VARCHAR(32),
    count_short     INTEGER,
    count_baseline  FLOAT,
    growth_rate     FLOAT,
    cross_source    INTEGER,                   -- 出现在几个数据源
    engagement_sum  INTEGER,
    kol_mention_cnt INTEGER DEFAULT 0,
    is_new_entity   BOOLEAN DEFAULT FALSE,
    sentiment_avg   FLOAT DEFAULT 0,
    final_score     FLOAT,
    rank            INTEGER,
    UNIQUE(window_end, window_type, entity)
);
CREATE INDEX idx_hot_window_rank ON hotness_snapshots(window_end DESC, rank ASC);
CREATE INDEX idx_hot_entity      ON hotness_snapshots(entity, window_end DESC);

-- ============================================================
-- L3 产出：共现关系
-- ============================================================
CREATE TABLE entity_cooccurrence (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    entity_a        VARCHAR(128) NOT NULL,
    entity_b        VARCHAR(128) NOT NULL,
    cooccur_count   INTEGER,
    baseline_count  FLOAT,
    growth_rate     FLOAT,
    is_new_pair     BOOLEAN DEFAULT FALSE,     -- 基线期从未共现过
    UNIQUE(window_end, entity_a, entity_b)
);
CREATE INDEX idx_cooc_window ON entity_cooccurrence(window_end DESC, growth_rate DESC);

-- ============================================================
-- L4 产出：话题簇
-- ============================================================
CREATE TABLE topic_clusters (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    cluster_label   VARCHAR(256),
    entities        TEXT[],
    msg_ids         BIGINT[],
    representative_msg_ids BIGINT[],           -- 精选的5条代表消息
    size            INTEGER,
    score           FLOAT,
    created_at      TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_tc_window ON topic_clusters(window_end DESC, score DESC);

-- ============================================================
-- L5 产出：叙事简报
-- ============================================================
CREATE TABLE narrative_briefs (
    id              BIGSERIAL PRIMARY KEY,
    cluster_id      BIGINT REFERENCES topic_clusters(id),
    entity          VARCHAR(128),
    window_end      TIMESTAMP NOT NULL,
    narrative       TEXT,
    catalyst        TEXT,
    key_players     TEXT[],
    sentiment       VARCHAR(16),
    fund_logic      TEXT,
    risk            TEXT,
    stage           VARCHAR(16),               -- 萌芽/爆发/高潮/衰退
    confidence      FLOAT,
    time_sensitivity VARCHAR(8),               -- 高/中/低
    raw_llm_output  JSONB,
    created_at      TIMESTAMP DEFAULT NOW()
);

-- ============================================================
-- 叙事生命周期追踪（跨时间窗口）
-- ============================================================
CREATE TABLE narrative_lifecycle (
    id              BIGSERIAL PRIMARY KEY,
    entity          VARCHAR(128) NOT NULL,
    first_seen      TIMESTAMP,
    peak_time       TIMESTAMP,
    peak_score      FLOAT,
    current_stage   VARCHAR(16),
    stage_history   JSONB,                     -- [{ts, stage, score}]
    is_active       BOOLEAN DEFAULT TRUE,
    updated_at      TIMESTAMP DEFAULT NOW()
);

-- ============================================================
-- 警报记录
-- ============================================================
CREATE TABLE alerts (
    id              BIGSERIAL PRIMARY KEY,
    entity          VARCHAR(128),
    trigger_type    VARCHAR(32),               -- growth_surge/new_narrative/cross_platform/new_entity
    growth_rate     FLOAT,
    score           FLOAT,
    payload         JSONB,
    is_sent         BOOLEAN DEFAULT FALSE,
    sent_at         TIMESTAMP,
    created_at      TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_alerts_unsent ON alerts(is_sent) WHERE is_sent = FALSE;
```

---

## 8. 模块目录与接口

### 8.1 目录结构

```
crypto_radar/
├── db/
│   ├── connection.py          [保留]
│   ├── models.py              [扩展：新增所有表的ORM]
│   └── migrations/            [Alembic迁移脚本]
│
├── scheduler/
│   └── jobs.py                [扩展：新增各层定时任务]
│
├── llm/
│   └── ollama_client.py       [保留，封装batch调用]
│
├── prompts/
│   ├── entity_extract.txt     [L1兜底prompt]
│   └── narrative_brief.txt    [L5简报prompt]
│
├── dictionaries/
│   ├── tickers.yaml           [代币词典，持续维护]
│   ├── chains.yaml            [链名词典]
│   ├── narratives.yaml        [叙事词典：AI Agent/RWA/etc]
│   ├── kols.yaml              [KOL账号+权重]
│   └── spam_patterns.yaml     [垃圾内容过滤规则]
│
├── services/
│   ├── prefilter.py           [保留，改造为输出初步实体标签]
│   │
│   ├── l0_normalizer.py       [归一化：字段统一、情绪词保留]
│   ├── l0_dedup.py            [SimHash + Embedding两阶段去重]
│   │
│   ├── l1_entity_extractor.py [三层实体抽取：正则+词典+模型]
│   ├── l1_sentiment.py        [情绪词提取与打分]
│   │
│   ├── l2_hotness.py          [增长率计算、综合评分、Top-K排行]
│   ├── l2_sliding_counter.py  [滑动窗口计数器（内存）]
│   ├── l2_alert_trigger.py    [实时激增检测与警报触发]
│   │
│   ├── l3_cooccurrence.py     [共现统计与新叙事检测]
│   ├── l3_lifecycle.py        [热点生命周期追踪]
│   │
│   ├── l4_embedding.py        [bge-m3向量化封装]
│   ├── l4_clustering.py       [HDBSCAN聚类 + 代表消息选择]
│   │
│   ├── l5_briefing.py         [定向简报生成（L4→L5）]
│   ├── l5_daily_report.py     [日报生成（所有简报→日报）]
│   │
│   └── l6_alerting.py         [警报推送：Telegram/企业微信]
│
├── ui/
│   └── streamlit_app.py       [可视化面板]
│
└── config.py                  [阈值配置、模型配置、数据源配置]
```

### 8.2 核心配置（config.py）

```python
# 热度检测阈值
GROWTH_THRESHOLDS = {
    'alert_critical': 20.0,
    'alert_high':     10.0,
    'top_k_include':   5.0,
}

# 时间窗口配置
WINDOWS = {
    'short': 1,      # 小时
    'baseline': 7,   # 天
    'smoothing': 2.0
}

# LLM配置
LLM_MODELS = {
    'entity_extract': 'qwen3:8b',
    'brief':          'qwen3:14b',
    'daily_report':   'qwen3:32b',
    'emergency':      'qwen3:8b',
}

# 每次简报处理的Top-K热点数
TOP_K_FOR_BRIEF = 20

# 代表消息数量
REPRESENTATIVE_MSGS = 5
```

---

## 9. 调度时序设计

```
时间轴（循环）：

:00  ─── L0+L1 持续处理新消息（流式，来一条处理一条）
         L2 滑动窗口内存实时更新

:00  ─── 每15分钟：L2批量计算增长率 → 更新hotness_snapshots
                   L3更新共现矩阵 → 检测新叙事
                   检查是否有激增警报触发

:00  ─── 每1小时：L4对Top-K热点实体跑Embedding聚类
                  L5对每个簇生成LLM简报
                  写入narrative_briefs
                  输出每小时热点简报文件

:00  ─── 每日：   L5用qwen3:32b跑日度总评
                  生成每日雷达报告
                  更新narrative_lifecycle（热点生命周期）
                  清理30天前的原始数据（保留热点记录）

实时触发（任意时刻）：
  增长率 > 20x → 立即触发L5紧急简报 → L6推送警报
  新词出现≥5次  → 标记，纳入下次L2计算

详细时序表：

| 任务 | 频率 | 触发方式 | 预计耗时 |
|------|------|---------|---------|
| L0+L1 消息处理 | 持续 | 队列消费 | <100ms/条 |
| L2 热度统计 | 每15分钟 | 定时 | <30s |
| L3 共现更新 | 每15分钟 | 定时 | <10s |
| L4 聚类 | 每1小时 | 定时 | 2~5min |
| L5 简报生成 | 每1小时 | L4完成后 | 10~30min（取决于Top-K数量）|
| L5 日报 | 每天1次 | 定时(凌晨) | 5~15min |
| L6 激增警报 | 实时 | 事件触发 | <5s |
| L6 文件输出 | 每1小时 | L5完成后 | <1s |
```

---

## 10. 技术栈选型

| 类别 | 选型 | 理由 |
|------|------|------|
| 数据库 | PostgreSQL（初期SQLite也可） | 成熟稳定，后期可加pgvector |
| 时序加速（后期） | ClickHouse | 亿级时序数据查询毫秒级 |
| 向量库 | FAISS（本地文件） | 轻量，别一开始就上Milvus |
| Embedding模型 | bge-m3 (sentence-transformers) | 中英混合最佳，2GB内存 |
| 近似去重 | datasketch（SimHash） | 成熟库，毫秒级 |
| 中文分词 | jieba | 轻量，可自定义词典（加入加密词汇） |
| 聚类 | HDBSCAN | 无需预设簇数，适合热点场景 |
| LLM框架 | Ollama（已有） | 复用现有环境 |
| 调度 | 现有scheduler | 复用 |
| 消息队列 | 内存队列（初期）/ Redis Queue（后期） | 初期不引入新依赖 |
| 可视化 | Streamlit | 快速搭建，Python原生 |
| 警报推送 | Telegram Bot API | 简单可靠 |
| ORM | SQLAlchemy（已有） | 复用 |

---

## 11. 硬件资源规划

**主机配置**：96G内存，纯CPU（无GPU）

| 组件 | 内存占用 | 备注 |
|------|---------|------|
| bge-m3 Embedding | ~2GB | 持续常驻，L0+L4共用 |
| qwen3:8b (Q4量化) | ~5GB | L1兜底+紧急简报 |
| qwen3:14b (Q4量化) | ~9GB | L5主力简报（常驻或按需加载）|
| qwen3:32b (Q4量化) | ~20GB | L5日报（仅每日1次，用后释放）|
| 滑动窗口内存计数器 | ~1~3GB | 取决于实体数量 |
| PostgreSQL | ~2~4GB | 数据库缓存 |
| Python进程 | ~1~2GB | 各服务进程 |
| **合计（日常）** | **~25~35GB** | 96G内存绰绰有余 |

**CPU推理速度参考**（纯CPU，qwen3:14b Q4）：
- 每次简报约500 token输出，约2~5分钟
- Top-20热点完整简报：约40~100分钟（可并行优化）
- 建议：简报生成设置为异步，不阻塞主流程

**优化建议**：
- bge-m3和qwen3:8b常驻内存，避免重复加载
- qwen3:32b用完立即释放（每天只跑1次）
- 聚类计算（HDBSCAN）主要消耗CPU，与LLM推理错开调度

---

## 12. 落地路径（分阶段）

### Phase 1：止血期（第1周）——立竿见影，无需LLM

**目标**：先把排行榜跑起来，哪怕没有LLM简报，你已经有可用的产品。

| Task | 模块 | 产出 | 优先级 |
|------|------|------|-------|
| 1.1 字段归一化 | l0_normalizer.py | 统一数据格式 | P0 |
| 1.2 SimHash去重 | l0_dedup.py | LLM调用量砍30~50% | P0 |
| 1.3 词典建立 v1 | dictionaries/*.yaml | 覆盖主流币+你关注的项目 | P0 |
| 1.4 正则+词典实体抽取 | l1_entity_extractor.py | 每条消息挂实体标签 | P0 |
| 1.5 增长率计算+排行 | l2_hotness.py | **每15分钟 Top-20 热度排行榜** ✅ | P0 |

**里程碑**：能看到实时热度排行榜，不依赖任何LLM。

---

### Phase 2：升级期（第2~3周）——质量跃迁

| Task | 模块 | 产出 | 优先级 |
|------|------|------|-------|
| 2.1 共现网络 | l3_cooccurrence.py | 新叙事检测 | P1 |
| 2.2 激增实时警报 | l2_alert_trigger.py + l6_alerting.py | Telegram推送 | P1 |
| 2.3 Embedding聚类 | l4_embedding.py + l4_clustering.py | 话题簇 | P1 |
| 2.4 LLM定向简报 | l5_briefing.py | 每小时结构化简报 | P1 |
| 2.5 情绪分析 | l1_sentiment.py | 情绪维度 | P2 |

**里程碑**：系统能自动生成结构化热点简报，并在激增时实时推送。

---

### Phase 3：精细化（第4周起）——打磨细节

| Task | 说明 |
|------|------|
| 3.1 热点生命周期追踪 | 自动识别萌芽/爆发/高潮/衰退阶段 |
| 3.2 KOL传播路径 | 谁先提，怎么扩散 |
| 3.3 Embedding升级为精筛 | SimHash → bge-m3两阶段去重 |
| 3.4 Streamlit面板 | 可视化排行榜+共现图+生命周期 |
| 3.5 日报生成 | qwen3:32b日度总评 |
| 3.6 词典持续维护 | 随市场热点扩充词典 |

---

### Phase 4：高级演进（长期）

| 功能 | 说明 |
|------|------|
| 链上数据接入 | Dune Analytics API，链上行为与社交共振 |
| KOL影响力动态评分 | 历史喊单准确率更新KOL权重 |
| 跨平台传播路径 | Twitter热→Discord爆→广场接盘 时序分析 |
| 自动词典扩充 | 新词检测后自动提示补充词典 |
| ClickHouse接入 | 数据量大后做时序加速 |

---

## 13. 反模式清单

在设计和开发过程中，**以下做法明确禁止**：

| 反模式 | 为什么错 | 正确做法 |
|--------|---------|---------|
| 把全部原始消息丢给LLM总结 | Token爆炸，LLM平均化，看不到小而急的新热点 | 先统计发现，再LLM解释 |
| 只看绝对提及量 | BTC/ETH永远第一，看不到新叙事 | 看增长率，不看绝对值 |
| 用LLM做去重 | 浪费算力，100ms能干的事别花10分钟 | SimHash/Embedding做去重 |
| 单源判断热点 | 容易被单平台噪声/水军误导 | 多源共振才是真信号 |
| 固定时间窗口触发 | 错过爆发期的前15分钟 | 实时激增检测+事件触发 |
| 把LLM当计数器用 | LLM不擅长统计，代码更快更准 | 统计交给代码，解释交给LLM |
| 一次把所有消息送给LLM聚类 | 该用Embedding+HDBSCAN做的事 | 向量聚类，LLM只看代表消息 |
| 忽略共现关系 | 只看单词频率，看不到叙事形成 | 建立共现网络，发现新组合 |

---

## 14. 后续演进路线

```
现在              3个月              6个月              1年
  │                 │                  │                 │
  ▼                 ▼                  ▼                 ▼
多源数据          热点雷达            叙事追踪          Alpha系统
+统计排行  →  +LLM简报+警报  →  +生命周期+KOL路径  →  +链上共振+预测
```

**终极形态**（你在做的本质）：

一个"加密世界信息压缩引擎"。

加密市场真正的alpha不是信息多，而是：**谁能更快从噪音中发现增长最快的叙事**。

你的系统如果做到Phase 3，就已经能持续稳定地在别人"感觉到热"之前若干小时，拿到结构化的信号。这是真实的信息优势。

---

*文档版本：v2.0 · 综合三方案精华*
*适用硬件：96G内存 · 纯CPU*
*核心设计理念：统计发现热点，LLM解释热点，代码干统计，LLM干理解*