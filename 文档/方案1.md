我把你的仓库看完了,所以先说结论:你现在的"预处理 → 一次摘要 → 二次摘要"方案**能跑**,但**不是最优解**,而且跟你的实际目标("抓近期热点")之间有一个关键的方法论错位。下面我先指出问题,再给你一套更贴合加密领域热点挖掘的方案,最后给你落地路径。

## 一、你现在方案的根本问题

**核心问题:LLM 摘要不等于热点发现。**

你现在是把"每 20 条原始数据"按时间顺序喂给模型做摘要。这个做法有三个结构性缺陷:

1. **批次边界是时间切的,不是话题切的。** 20 条推文里可能涉及 15 个不同项目,模型只能流水账式地"这个说了 A,那个说了 B",既不知道哪个是热点,也不知道哪个只是一个人的自言自语。
2. **没有跨条目去重。** 加密社交数据有 30%-50% 是转推、复读、同一条新闻的不同 KOL 转述。模型被迫反复读同一件事,还会误以为"这件事被提到很多次 = 重要",其实只是复读机效应,跟真正的热点不是一个量级。
3. **没有"谁被提到"这个维度。** 热点的本质是 **某个实体(代币 / 项目 / 事件)在某个时间窗内出现频率 / 传播速度异常。** 你现在的 prompt 是让模型自己从文本里"感觉"这件事,对小模型来说不靠谱,对大模型来说是浪费推理算力。

一句话:**LLM 擅长"把一段文字浓缩成摘要",不擅长"从 10 万条里统计出谁最火"。后者本质是信息检索 + 聚合的活,该让代码干,别让模型干。**

## 二、更合适的方案:实体驱动的热点雷达

思路是把 pipeline 重新切成四层,LLM 只在最后一层介入,而且只处理"已经被判定为热点"的子集。

```
原始数据
  ↓ (1) 归一 + 近似去重          ← 纯代码 / 轻量向量
  ↓ (2) 实体抽取 + 时间窗聚合    ← 规则 + 词典为主,小模型兜底
  ↓ (3) 热度打分 + 候选排序      ← 纯数学
  ↓ (4) 对 Top-K 热点做定向 LLM 简报 + 跨热点聚合
最终产出:今日加密雷达
```

### 各层该干什么

**(1) 归一 + 去重**
- 统一字段(text / author / ts / source / engagement)、清掉纯链接 / 纯 emoji 行。
- 近似去重必做。两种做法二选一:
  - **便宜路线**:SimHash / MinHash,Python 里 `datasketch` 就够,毫秒级。
  - **高质量路线**:本地跑一个嵌入模型(比如 `bge-m3`,2GB 左右,96G 内存绰绰有余),cosine ≥ 0.92 判为重复。你还能顺便拿到向量用在后面聚类。
- 对加密数据**强烈建议走嵌入路线**,因为大量"同一个消息不同翻译/不同措辞"的情况 SimHash 抓不住。

**(2) 实体抽取**

这一步是最有杠杆的地方,加密领域有天然的"抓手":
- **强信号(正则就能抓)**:`$TICKER`、合约地址(`0x...` / Solana base58)、项目 Twitter handle、K 线关键词。
- **词典**:维护一份"主流币 + 你关注的长尾项目"词典,按别名 / 中文名 / 英文名都挂上。
- **模糊/长尾**:在你的 prefilter 里已经有雏形了,可以再加一个"未命中任何词典但含特征词"的 bucket,后面让小模型批量标注。

抽完每条消息都挂上 0-N 个实体标签。

**(3) 热度打分**

对每个实体,在滑动时间窗(1h / 6h / 24h)里算:
- 提及次数 `count`
- **加速度**:`count(当前窗) / count(基线窗)` —— 这才是"近期变热"的核心指标,纯 count 只能看出"一直热"的 BTC/ETH。
- 跨源覆盖:Twitter + Binance + Discord 同时提到,权重更高(抗单一平台噪声)。
- 可选:如果原始数据里有互动量(点赞 / 转发 / 回复),加权进去。

排序后取 Top-K(比如每个窗口取 20 个实体)。这一步**完全是数学,不需要 LLM**。

**(4) 对 Top-K 做定向 LLM 简报**

现在才轮到 LLM,而且任务变得**非常聚焦**:
- 对每个热点实体 X,从它的消息池里挑 N 条最具代表性的(去重后按互动量或多样性采样,比如用嵌入做一次 K-means 取簇心)。
- prompt 模板固定成结构化输出,比如:
  ```
  关于 $X,最近 6 小时发生了什么?
  请给出:
  - 核心叙事(1-2 句)
  - 触发事件 / 催化剂(如果有)
  - 社区情绪(正面 / 负面 / 中性 + 1 句话)
  - 代表性原文(最多 3 条)
  ```
- 这种 prompt 对 8B 小模型友好,产出质量也远高于"给 20 条无关内容让它自己总结"。

**(5) 可选的二次聚合(对应你的 level2)**

把 Top-K 简报再喂给一次大模型,让它做**主题聚类和总评**(例如"今天的主题是 L2 项目遇冷 + Memecoin 反弹"),而不是单纯再做一次摘要。这一步才是你 96G 内存主机跑大模型的正确时机:低频、高质量、输入已经是高密度的。

## 三、你的硬件适合怎么配

96G 内存、没说 GPU 我就当是纯 CPU:

| 用途 | 模型建议 | 大小(Q4) | 说明 |
|------|---------|---------|------|
| 嵌入(去重 + 聚类) | `bge-m3` 或 `Qwen3-Embedding-0.6B` | 1-2GB | 中英混合都稳,长文本友好 |
| 实体抽取兜底 | 你现在的 `qwen3:8b` | ~5GB | 基本够,主要让它做结构化抽取不是自由写作 |
| 热点定向简报(level1) | `qwen3:8b` 或 `qwen3:14b` | 5-9GB | 你现在用的就行 |
| 主题聚合(level2) | `qwen2.5:32b` / `qwen3:32b` Q4 | ~20GB | 低频跑,质量拉满 |

CPU 推理瓶颈是 token/s,不是内存。**你真正的省钱点是"尽量少让 LLM 跑"**,这恰恰是上面方案的重点——LLM 只处理 Top-K 热点,不处理原始 stream。

## 四、和你现有代码的迁移路径

好消息是你的工程骨架(SQLAlchemy / 串行 worker / prompt 模板 / prefilter)**完全不用扔**,只需要在现有分层里插两步、改一步:

1. **保留**:`db/connection.py`、`scheduler/jobs.py`、`llm/ollama_client.py`、prompts 目录、service 骨架。
2. **新增**:
   - `services/dedup.py`(SimHash 或嵌入近似去重)
   - `services/entity_extractor.py`(词典 + 正则 + 可选小模型兜底)
   - `services/hotness.py`(时间窗聚合 + 打分)
   - 新表 `entity_mentions(entity, source, raw_id, ts)` + `hot_topics(window_start, window_end, entity, score, sample_ids, brief)`
3. **改造**:`level1_service` 从"批量摘要"改成"按热点实体出简报";`level2_service` 从"摘要再摘要"改成"主题聚类总评"。
4. **prefilter 提级**:你现在的 prefilter 基本可以直接复用,但把它的输出从"保留/丢弃"扩展成"保留 + 附加初步实体标签",能省掉后面一次扫描。

## 五、优先级建议

如果时间有限,按这个顺序做 ROI 最高:

1. **先加近似去重**——最立竿见影,LLM 调用量可能直接砍掉 30-50%。
2. **再加实体抽取 + 热度打分**——让系统能输出"Top 10 热点"这个核心产物,哪怕还没接 LLM 简报,你已经每天能看一眼排行榜了。
3. **最后改造 LLM 层**——从"按批摘要"切到"按热点出简报",质量会有明显跃迁。
4. 二次摘要(level2)最后再改,现阶段那一层不是瓶颈。

---

要不要我先聚焦做第 1 步(去重层)?或者你想先看看完整的新 schema + pipeline 骨架代码,我可以写一版 design 文档放到 `.kiro/specs` 里,我们按 spec 流程一步步推。你挑一个方向,我动手。