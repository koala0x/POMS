# 加密叙事雷达系统 · 终极方案 v3.0

> 综合 **方案4（极简主义）** + **设计文档1（骨架）** + **设计文档2（完整架构）** 的精华融合版。
>
> **项目定位**：不是"新闻摘要器"，不是"行情播报机"，而是 **加密注意力流动探测器**。
>
> **核心使命**：从海量社交噪音中，**最快**发现"正在从冷到热的新共识"——在市场看懂之前，你已经看到了。

---

## 目录

0. [设计哲学与红线](#0-设计哲学与红线)
1. [项目定位与真正要解决的问题](#1-项目定位与真正要解决的问题)
2. [三句话核心原则](#2-三句话核心原则)
3. [信号价值层级](#3-信号价值层级)
4. [系统总架构（七层漏斗）](#4-系统总架构七层漏斗)
5. [数据流：一条消息的完整旅程](#5-数据流一条消息的完整旅程)
6. [各层详细设计](#6-各层详细设计)
7. [关键算法](#7-关键算法)
8. [数据库设计（分阶段）](#8-数据库设计分阶段)
9. [模块目录与接口](#9-模块目录与接口)
10. [调度时序](#10-调度时序)
11. [技术栈](#11-技术栈)
12. [硬件资源规划](#12-硬件资源规划)
13. [分阶段落地路径](#13-分阶段落地路径)
14. [反模式清单](#14-反模式清单)
15. [给个人开发者的忠告（方案4精华）](#15-给个人开发者的忠告方案4精华)
16. [后续演进](#16-后续演进)

---

## 0. 设计哲学与红线

### 0.1 你真正在做的事

你正在做的**不是**：
- ❌ 一个漂亮的微服务架构
- ❌ 一个完整的加密信息百科
- ❌ 一个"LLM总结工具"

你真正在做的是：
- ✅ **一个能持续稳定地、比市场更早发现叙事迁移的雷达**

这是两个完全不同的方向。前者是技术炫技，后者是信息优势。

### 0.2 三条红线（必须守住）

**红线一：不要过度设计。**

方案4说得很清楚：个人开发者最大的敌人不是技术难度，而是**系统复杂度**。Milvus、微服务、ClickHouse、一堆队列——这些让你"永远在调架构，永远没跑出Alpha"。

**红线二：不要把LLM当发现工具。**

LLM擅长**解释**热点，不擅长**发现**热点。发现是统计问题，该交给代码。

**红线三：不要先求"全"，要先求"早"。**

雷达的价值在"早"，不在"全"。覆盖10%的新叙事但比市场早6小时，比覆盖100%但比市场晚6小时值钱100倍。

---

## 1. 项目定位与真正要解决的问题

### 1.1 你已经有的

- 多源数据持续流入：Twitter / Discord / 币安广场
- 现有 pipeline：Python预处理 → LLM一次摘要 → LLM二次摘要
- 本地主机：96G内存，纯CPU
- 工程骨架：SQLAlchemy、scheduler、Ollama、prefilter、repositories

### 1.2 现有方案的三个结构性缺陷

| 缺陷 | 表现 | 根因 |
|------|------|------|
| **批次按时间切，不按话题切** | 20条推文涉及15个项目，模型只能流水账 | 缺聚类 |
| **没有跨条目去重** | 30~50%是转推复读，LLM反复读同一事 | 缺SimHash/Embedding |
| **LLM被用错了地方** | 让LLM从10万条里"数"谁最火 | 统计任务应交给代码 |

**最关键的认知错误**：LLM在总结时会"平均化"——优先总结主流，忽略占比3%但增长80倍的新词。而真正的Alpha恰恰在那3%里。

### 1.3 真正要解决的问题

```
不是：怎么总结更多内容
而是：怎么在噪音中最快发现"从冷到热的信号"
```

一句话：**发现"变化"，而不是"总结内容"**。

---

## 2. 三句话核心原则

**① 热点 = 增长率，不是绝对值**

BTC永远高频，但真正的Alpha是从冷到热的爆发。`$LAUNCHCOIN` 平均每小时20次，突然变成900次——**这45倍才是信号**，不是BTC稳定的5000次。

**② LLM只在最后一公里介入**

统计的活让代码干，检索的活让向量干，LLM只做一件事：对**已经被数学确认为热点**的内容，用人话解释"为什么热、热在哪、怎么看"。

**③ 做雷达，不做百科**

雷达的价值在于发现**新出现的信号**，不在描述全貌。系统的核心竞争力是**早**，不是**全**。

---

## 3. 信号价值层级

按重要性从高到低排序，也是系统投入的优先级：

| # | 信号类型 | 描述 | 实现层 | 落地阶段 |
|---|---------|------|-------|---------|
| 1 | **增长率激增** | 某实体短窗口提及量 vs 长期基线 | L2 | **Phase 1（P0）** |
| 2 | **多源共振** | 同一实体在Twitter + Discord + 广场同时热 | L2 | Phase 1 |
| 3 | **新词突现** | 基线期从未出现，突然高频 | L2 | Phase 1 |
| 4 | **共现新组合** | 两个词突然频繁一起出现（新叙事形成） | L3 | Phase 2 |
| 5 | **KOL首发/扩散链** | 谁先提、怎么扩散 | L1+L2 | Phase 2 |
| 6 | **情绪转向** | 同一实体情绪从中性急转正面 | L2 | Phase 2 |
| 7 | **叙事生命周期** | 萌芽→爆发→高潮→衰退的阶段识别 | L3 | Phase 3 |

**注意**：前3个信号（增长率、共振、新词）**不需要任何LLM、不需要Embedding、不需要聚类**。纯代码就能跑出来，而且已经是可用产品。

---

## 4. 系统总架构（七层漏斗）

```
┌─────────────────────────────────────────────────────────────────┐
│  数据源：Twitter/X · Discord · 币安广场 · (未来: Telegram/链上)  │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 原始消息流
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L0  归一化 + 近似去重                                           │
│      纯代码 · SimHash粗筛 → (后期)Embedding精筛                   │
│      ► 去重后的标准化消息（砍掉30~50%重复）                      │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L1  实体抽取 + 情绪词提取                                       │
│      正则抓$TICKER · 词典匹配 · (可选)小模型兜底长尾             │
│      ► 每条消息挂 0~N 个实体标签 + 情绪词                        │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L2  热度统计层  ⭐ 系统核心（Phase 1必做）                       │
│      增长率 · 跨源加权 · 情绪强度 · 新词检测                     │
│      ► Top-K 热点实体排行 + 实时激增警报                         │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L3  共现网络层  ⭐ 叙事发现核心（Phase 2）                       │
│      实体两两共现统计 · 共现增长率 · 三角共现                     │
│      ► 正在形成的叙事组合（AI Agent + Base + Virtuals）          │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L4  Embedding 聚类层（Phase 2）                                 │
│      bge-m3向量化 · HDBSCAN聚类 · 簇代表消息提取                 │
│      ► 话题簇（说同一件事的消息归为一簇）                        │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L5  LLM 定向简报层（Phase 2）                                   │
│      Qwen3 8B/14B · 固定JSON模板 · 只处理Top-K簇                 │
│      ► 每个热点的结构化简报（叙事/催化剂/龙头/情绪/风险/阶段）   │
└──────────────────────────┬──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  L6  产出层                                                      │
│      每日雷达报告 · 实时激增警报 · Streamlit可视化面板            │
└─────────────────────────────────────────────────────────────────┘
```

### 4.1 LLM介入的精确位置

整个pipeline中，**LLM只在两处介入**：

- **L1 兜底**（可选）：未命中词典的消息做实体抽取，batch处理，低频
- **L5 简报**（主力）：对数学确认的Top-K话题簇做结构化总结

LLM的日调用量级 = **几十到几百次**（不是几万次）。

### 4.2 七层中的"MVP核心"vs"可选增强"

| 层 | 是否MVP必做 | 备注 |
|---|---|---|
| L0 归一化 | ✅ 必做 | 字段统一，最小闭环的基础 |
| L0 SimHash去重 | ✅ 必做 | 立即减30~50% LLM成本 |
| L0 Embedding去重 | ⏸ 后期 | Phase 3升级 |
| L1 正则+词典 | ✅ 必做 | 核心 |
| L1 LLM兜底 | ⏸ 后期 | 词典够用就先不上 |
| L1 情绪分析 | ⏸ 后期 | Phase 2 |
| **L2 增长率+排行** | ✅✅ 最必做 | **整个产品的心脏** |
| L2 实时激增警报 | ✅ Phase 1末 | 几乎就是顺手加 |
| L3 共现网络 | ⏸ Phase 2 | 新叙事发现的灵魂 |
| L3 生命周期 | ⏸ Phase 3 | 打磨 |
| L4 Embedding | ⏸ Phase 2 | LLM简报前的必要准备 |
| L4 HDBSCAN | ⏸ Phase 2 | 配合L5 |
| L5 LLM简报 | ⏸ Phase 2 | 没它也能跑 |
| L5 日度总评 | ⏸ Phase 3 | 锦上添花 |
| L6 Telegram警报 | ✅ Phase 1末 | 简单实用 |
| L6 Streamlit面板 | ⏸ Phase 3 | 可视化 |

---

## 5. 数据流：一条消息的完整旅程

```
[原始消息]
  "Just aped into $LAUNCHCOIN, AI Agent narrative on Base is heating up,
   Virtuals ecosystem exploding 🚀🚀🚀"
  source=twitter · author=@kol_xxx · ts=2026-05-10T10:23:00 · likes=1520

  ↓ L0 归一化
  清洗emoji（保留🚀作情绪信号）· 统一字段 · engagement=1520

  ↓ L0 去重（SimHash）
  汉明距离与过去24h对比 → 无重复，通过

  ↓ L1 实体抽取
  正则命中: $LAUNCHCOIN
  词典命中: [Base, Virtuals, AI Agent]
  情绪词:   [aped, heating up, exploding, 🚀×3] → sentiment=+0.85
  KOL标记:  @kol_xxx 粉丝>5万 → author_weight=2.5

  ↓ 写入 entity_mentions（4条记录）

  ↓ L2 热度统计（每15分钟批量 + 事件触发）
  $LAUNCHCOIN: 1h=900,  7d均/h=20  → growth=45x  ⚡critical
  Base:        1h=340,  7d均/h=280 → growth=1.2x  正常
  AI Agent:    1h=520,  7d均/h=85  → growth=6.1x  激增
  Virtuals:    1h=290,  7d均/h=40  → growth=7.2x  激增
  跨源: $LAUNCHCOIN 在 Twitter+Discord 同时热 → cross_source=2

  ↓ L3 共现网络
  ($LAUNCHCOIN, AI Agent) 共现增长=38x → 新叙事候选 ⭐
  (AI Agent, Virtuals, Base) 三角共现增长=22x → 新叙事确认 ⭐

  ↓ L4 Embedding聚类
  $LAUNCHCOIN 的830条消息 bge-m3向量化
  HDBSCAN聚出3簇：
    Cluster_A(620条)：主叙事，AI Agent+Base生态
    Cluster_B(140条)：价格讨论
    Cluster_C(70条) ：技术讨论
  Cluster_A 挑 5 条代表消息

  ↓ L5 LLM简报（qwen3:14b）
  {
    "narrative": "新AI Agent基础设施项目在Base链上爆发，Virtuals生态带动LAUNCHCOIN",
    "catalyst": "Virtuals V2路线图发布 + 头部KOL @kol_xxx 喊单",
    "key_players": ["$LAUNCHCOIN", "$VIRTUAL", "Base"],
    "sentiment": "极度看多",
    "fund_logic": "AI Agent资金从ETH向Base迁移，LAUNCHCOIN是弹性最高的新标的",
    "risk": "KOL驱动，无基本面支撑，砸盘风险",
    "stage": "爆发期",
    "confidence": 0.82,
    "time_sensitivity": "高"
  }

  ↓ L6 产出
  写入 narrative_briefs
  增长率>20x → Telegram紧急警报
  纳入当日雷达报告
```

---

## 6. 各层详细设计

### L0 归一化与去重

**目标**：数据量砍30~50%，消除复读机效应。

**归一化字段**：

```python
@dataclass
class NormalizedMessage:
    text: str
    author: str
    author_weight: float   # 0.1 ~ 3.0
    ts: datetime           # UTC
    source: str            # twitter / discord / binance_square
    engagement: int        # 归一化后的点赞+转发+回复
    raw_id: int
```

**清洗规则**：

- 移除纯链接/纯表情/少于5个有效词的帖子
- 保留表情作为情绪信号（🚀 💀 🤔）
- 清洗广告账号（低粉+高频+固定模板）
- 统一 `$TICKER` 大写

**两阶段去重**：

| 阶段 | 方法 | 触发阶段 |
|---|---|---|
| **第一阶段** | SimHash（64位，汉明距离≤3判重） | **Phase 1 必做** |
| **第二阶段** | bge-m3 Embedding（cosine ≥ 0.92判重） | Phase 3 升级 |

同一内容保留互动量最高那条，用 `dup_of` 关联。

---

### L1 实体抽取

**目标**：让每条消息带上"说的是谁/什么"的标签。

**三层抽取（优先级从上到下）**：

1. **正则**（毫秒级，必做）
   ```
   $TICKER      \$[A-Z]{1,10}\b
   EVM地址      0x[a-fA-F0-9]{40}
   Solana地址   [1-9A-HJ-NP-Za-km-z]{32,44}
   Twitter     @[a-zA-Z0-9_]{1,15}
   ```

2. **词典匹配**（毫秒级，必做）

   `dictionaries/` 下的YAML词典，支持代币/链/叙事/KOL/交易所：

   ```yaml
   # tickers.yaml
   BTC: { aliases: [比特币, bitcoin, 大饼, 老大], type: layer1 }
   HYPE: { aliases: [hyperliquid, HL], type: defi }

   # narratives.yaml
   AI_Agent: { keywords: [AI agent, autonomous agent, ai16z], type: narrative }
   RWA: { keywords: [real world asset, tokenized, RWA], type: narrative }
   ```

3. **LLM兜底**（分钟级，选做）

   未命中任何词典的消息，积攒batch（50条/次）送给 `qwen3:8b`：
   ```
   从以下消息中提取所有代币名/项目名/链名/协议名/KOL。
   仅输出JSON：{"entities":[{"name":"xxx","type":"ticker/chain/project/kol"}]}
   ```

**confidence**：词典=1.0 · 正则=0.95 · 模型=0.6~0.85

**情绪词同步提取**：

```python
BULLISH_SIGNALS = {
    'strong': ['ape', 'exploding', 'moon', '100x', '🚀🚀🚀', 'send it'],
    'mild':   ['bullish', 'heating up', 'watching', 'accumulate']
}
BEARISH_SIGNALS = {
    'strong': ['rug', 'exit scam', 'dump', 'rekt', '💀'],
    'mild':   ['bearish', 'careful', 'risky', 'overbought']
}
```

---

### L2 热度统计与激增检测（⭐ 系统核心）

> **这是整个系统的心脏。它决定了你能看到什么，以及多快看到。**

**核心指标：增长率**

```python
def compute_growth_rate(entity, window_end, short_hours=1, baseline_days=7):
    short_count = count_mentions(entity, window_end - short_hours, window_end)
    baseline_total = count_mentions(
        entity,
        window_end - timedelta(days=baseline_days),
        window_end - timedelta(hours=short_hours)
    )
    baseline_per_hour = baseline_total / (baseline_days * 24 - short_hours)
    SMOOTHING = 2.0   # 平滑因子，防冷启动
    return short_count / max(baseline_per_hour, SMOOTHING)
```

**多窗口并行**（捕获不同周期）：

| 窗口 | 用途 |
|---|---|
| 1h vs 7天均值 | 突发热点（主力窗口） |
| 6h vs 30天均值 | 中期上升趋势 |
| 15min vs 24h均值 | 超短期脉冲（配合实时警报） |

**综合热度评分**：

```python
def final_score(record):
    growth   = record.growth_rate                      # 主信号
    cross    = 1 + 0.3 * (record.cross_source - 1)    # 跨源加权
    eng      = math.log1p(record.engagement_sum)       # 互动对数加权
    kol      = 1 + 0.5 * record.kol_mention_flag       # KOL加权
    novelty  = 2.0 if record.is_new_entity else 1.0    # 新词×2
    return growth * cross * eng * kol * novelty
```

**新词检测**（特殊信号）：

```python
def detect_new_entities(window_end):
    """基线期7天前从未出现，最近1h突然≥5次 → bypass增长率直接入榜"""
    recent = get_entities_in_window(window_end - 1h, window_end)
    historical = get_entities_in_window(window_end - 7d, window_end - 1h)
    return [e for e in recent if e not in historical and recent[e] >= 5]
```

**实时激增触发器**（事件驱动，不等定时任务）：

```python
ALERT_THRESHOLDS = {
    'critical': 20.0,   # >20x 立即推送Telegram
    'high':     10.0,   # >10x 纳入下次简报
    'medium':    5.0,   # >5x  纳入定期排行
}
```

---

### L3 共现网络与叙事发现

> **这是系统最有远见的一层。单词增长率告诉你"谁热"，共现告诉你"新叙事在形成"。**

**共现统计**：

```python
def update_cooccurrence(message):
    # 消息含 [LAUNCHCOIN, AI Agent, Base, Virtuals]
    # → 产生 6 对共现
    for a, b in combinations(sorted(message.entities), 2):
        increment_cooccurrence(a, b, message.ts, message.source)
```

**新叙事检测**：

```python
def detect_emerging_narratives(window_end):
    hot = get_top_k_entities(window_end, k=30)
    emerging = []
    for a, b in combinations(hot, 2):
        cur  = cooccur_count(a, b, window_end - 1h, window_end)
        base = cooccur_count(a, b, window_end - 7d, window_end - 1h) / (7 * 24)
        if cur >= 5 and cur / max(base, 0.5) > 5.0:
            emerging.append({'pair': (a, b), 'growth': cur / max(base, 0.5)})
    # 进一步找三角共现（3个词同时） → 叙事更成熟
    triangles = find_triangle_cooccurrence(hot, window_end)
    return sorted(emerging, key=lambda x: x['growth'], reverse=True)
```

**叙事生命周期**：

```python
NARRATIVE_STAGES = {
    'emerging':  lambda r: 2 < r < 5,              # 萌芽期
    'breakout':  lambda r: 5 <= r < 15,            # 爆发期（核心入场窗口）
    'peak':      lambda r: r >= 15,                # 高潮期（谨慎追高）
    'declining': lambda r, prev: r < 2 and prev > 5 # 衰退期
}
```

---

### L4 Embedding 聚类

**目标**：把"说同一件事"的消息归为一簇，每簇挑5条代表送LLM。

**模型选择**：`bge-m3`（BAAI/bge-m3）
- 中英混合最佳
- 1024维，2GB内存，CPU推理可接受
- 支持8192 token输入

**聚类算法**：HDBSCAN

为什么不用K-Means或DBSCAN：
- 不需要预设簇数（你不知道今天有几个热点）
- 能识别噪声点（-1标签=孤立消息）
- 对簇大小差异不敏感

```python
from hdbscan import HDBSCAN

clusterer = HDBSCAN(
    min_cluster_size=5,
    min_samples=3,
    metric='euclidean',
    cluster_selection_method='eom'
)
```

**代表消息选择（多样性+权威性）**：

```python
def select_representatives(embeddings, msg_ids, engagements, n=5):
    centroid = embeddings.mean(axis=0)
    # 50% 取距簇心最近的（代表性）
    by_distance = sorted(zip(cosine_distances([centroid], embeddings)[0], msg_ids))[:n//2 + 1]
    # 50% 取互动量最高的（权威性）
    by_engagement = sorted(zip(engagements, msg_ids), reverse=True)[:n//2 + 1]
    candidates = set([m for _, m in by_distance] + [m for _, m in by_engagement])
    # 最终cosine>0.95的去重
    return deduplicate_by_similarity(candidates, 0.95)[:n]
```

---

### L5 LLM 定向简报

**模型配置**：

| 用途 | 模型 | 频率 |
|---|---|---|
| 热点简报（主力） | qwen3:14b | 每1~2小时，处理Top20 |
| 实时紧急简报 | qwen3:8b | 增长率>20x立即触发 |
| 日度总评 | qwen3:32b (Q4) | 每天1次 |

**主Prompt模板**（固定结构化输出）：

```
系统角色：你是加密货币市场的叙事分析师，擅长识别正在形成的新共识。

以下是关于【{entity}】的热点数据：

[量化信号]
- 最近1小时提及：{short_count}次
- 过去7天均值：{baseline}/h
- 增长倍数：{growth_rate}x
- 跨平台分布：{sources}
- 综合热度分：{score}

[共现叙事]
{entity}最近频繁与以下概念共现：{cooccurrence_pairs}

[代表消息]
{sample_messages}

[任务]
输出严格JSON，无其他文字：
{
  "narrative":       "≤50字，核心叙事",
  "catalyst":        "触发这波的具体催化剂",
  "key_players":     ["龙头项目/代币，最多3个"],
  "sentiment":       "极度看多/看多/中性/看空/极度看空",
  "fund_logic":      "≤80字，资金关注逻辑",
  "risk":            "≤50字，主要风险",
  "stage":           "萌芽期/爆发期/高潮期/衰退期",
  "confidence":      0.0-1.0,
  "time_sensitivity":"高/中/低"
}
```

**日度总评Prompt**（L6用qwen3:32b）：

```
以下是今日{n}条热点简报汇总：
{all_briefs}

输出JSON：
{
  "dominant_narratives":  ["今日主导叙事，按重要性排序"],
  "emerging_signals":     ["明日重点关注的早期信号"],
  "sentiment_overview":   "整体市场情绪",
  "narrative_rotation":   "资金在哪些叙事间轮动",
  "key_risks":            ["最需警惕的风险"],
  "tomorrow_watchlist":   ["明日重点观察的实体"]
}
```

---

### L6 产出与警报

**① 实时激增警报**（Telegram Bot）

```
🚨 叙事激增警报

实体：$LAUNCHCOIN
增长率：45x（过去1h 900次 vs 均值20次/h）
跨平台：Twitter + Discord 同时热
共现叙事：AI Agent + Base + Virtuals

⚡ 阶段：爆发期
📊 时效性：高

[查看完整简报]
```

**② 每2小时热点简报**（Markdown）

Top 10热点 + 每个热点的结构化简报

**③ 每日雷达报告**（Markdown）

主导叙事 + 叙事轮动 + 明日观察清单 + 风险提示

**④ Streamlit面板**

- 实时热度排行榜（柱状图自动刷新）
- 共现网络图（力导向）
- 热点生命周期时间轴
- 多源情绪对比

---

## 7. 关键算法

### 7.1 SimHash 去重

```python
import hashlib

def simhash(text, bits=64):
    tokens = tokenize(text)  # jieba分词
    v = [0] * bits
    for token in tokens:
        h = int(hashlib.md5(token.encode()).hexdigest(), 16)
        for i in range(bits):
            v[i] += 1 if h & (1 << i) else -1
    return sum(1 << i for i in range(bits) if v[i] > 0)

def hamming_distance(h1, h2):
    return bin(h1 ^ h2).count('1')

def is_duplicate(h1, h2, threshold=3):
    return hamming_distance(h1, h2) <= threshold
```

### 7.2 滑动窗口计数器（内存，高效）

```python
from collections import defaultdict, deque
import time

class SlidingWindowCounter:
    """
    纯内存实现，重启后从DB重建
    对高频统计，比每次查DB快100倍+
    """
    def __init__(self):
        self.windows = {w: defaultdict(deque) for w in
                        ['15min', '1h', '6h', '24h', '7d']}
        self.window_seconds = {
            '15min': 900, '1h': 3600, '6h': 21600,
            '24h': 86400, '7d': 604800,
        }

    def add(self, entity, ts=None):
        ts = ts or time.time()
        for w in self.windows:
            self.windows[w][entity].append(ts)

    def count(self, entity, window='1h'):
        now = time.time()
        cutoff = now - self.window_seconds[window]
        deq = self.windows[window][entity]
        while deq and deq[0] < cutoff:
            deq.popleft()
        return len(deq)

    def growth_rate(self, entity, short='1h', baseline='7d'):
        sc = self.count(entity, short)
        bt = self.count(entity, baseline)
        bh = self.window_seconds[baseline] / 3600
        return sc / max(bt / bh, 2.0)
```

### 7.3 共现矩阵（稀疏、滑窗）

```python
from itertools import combinations

class CooccurrenceTracker:
    def __init__(self):
        self.counters = defaultdict(SlidingWindowCounter)

    def update(self, entities, ts=None):
        if len(entities) < 2:
            return
        for a, b in combinations(sorted(entities), 2):
            self.counters[frozenset({a, b})].add('count', ts)

    def growth(self, a, b):
        c = self.counters.get(frozenset({a, b}))
        return c.growth_rate('count', short='1h', baseline='7d') if c else 0

    def top_emerging(self, hot_entities, top_n=20, threshold=3.0):
        results = [((a, b), self.growth(a, b))
                   for a, b in combinations(hot_entities, 2)
                   if self.growth(a, b) > threshold]
        return sorted(results, key=lambda x: x[1], reverse=True)[:top_n]
```

---

## 8. 数据库设计（分阶段）

**选型**：PostgreSQL（初期SQLite也可撑，日均百万条以内无压力）

### 8.1 Phase 1（MVP）必建表：4 张

```sql
-- 标准化消息（L0 产出）
CREATE TABLE normalized_messages (
    id            BIGSERIAL PRIMARY KEY,
    raw_id        BIGINT,
    text          TEXT NOT NULL,
    author        VARCHAR(128),
    author_weight FLOAT DEFAULT 1.0,
    source        VARCHAR(32) NOT NULL,
    ts            TIMESTAMP NOT NULL,
    engagement    INTEGER DEFAULT 0,
    simhash       BIGINT,
    is_duplicate  BOOLEAN DEFAULT FALSE,
    dup_of        BIGINT,
    created_at    TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_norm_ts ON normalized_messages(ts DESC);
CREATE INDEX idx_norm_source ON normalized_messages(source, ts DESC);
CREATE INDEX idx_norm_simhash ON normalized_messages(simhash);

-- 实体提及（L1 产出，L2 消费）
CREATE TABLE entity_mentions (
    id              BIGSERIAL PRIMARY KEY,
    msg_id          BIGINT REFERENCES normalized_messages(id),
    entity          VARCHAR(128) NOT NULL,
    entity_type     VARCHAR(32),
    source          VARCHAR(32) NOT NULL,
    ts              TIMESTAMP NOT NULL,
    engagement      INTEGER DEFAULT 0,
    author_weight   FLOAT DEFAULT 1.0,
    confidence      FLOAT DEFAULT 1.0,
    sentiment_score FLOAT DEFAULT 0,
    is_kol_mention  BOOLEAN DEFAULT FALSE
);
CREATE INDEX idx_em_entity_ts ON entity_mentions(entity, ts DESC);
CREATE INDEX idx_em_ts        ON entity_mentions(ts DESC);

-- 热度快照（L2 产出）
CREATE TABLE hotness_snapshots (
    id              BIGSERIAL PRIMARY KEY,
    window_end      TIMESTAMP NOT NULL,
    window_type     VARCHAR(16) NOT NULL,
    entity          VARCHAR(128) NOT NULL,
    entity_type     VARCHAR(32),
    count_short     INTEGER,
    count_baseline  FLOAT,
    growth_rate     FLOAT,
    cross_source    INTEGER,
    engagement_sum  INTEGER,
    kol_mention_cnt INTEGER DEFAULT 0,
    is_new_entity   BOOLEAN DEFAULT FALSE,
    sentiment_avg   FLOAT DEFAULT 0,
    final_score     FLOAT,
    rank            INTEGER,
    UNIQUE(window_end, window_type, entity)
);
CREATE INDEX idx_hot_window_rank ON hotness_snapshots(window_end DESC, rank ASC);

-- 警报记录
CREATE TABLE alerts (
    id            BIGSERIAL PRIMARY KEY,
    entity        VARCHAR(128),
    trigger_type  VARCHAR(32),
    growth_rate   FLOAT,
    score         FLOAT,
    payload       JSONB,
    is_sent       BOOLEAN DEFAULT FALSE,
    sent_at       TIMESTAMP,
    created_at    TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_alerts_unsent ON alerts(is_sent) WHERE is_sent = FALSE;
```

### 8.2 Phase 2 新增：3 张

```sql
-- 共现关系（L3）
CREATE TABLE entity_cooccurrence (
    id             BIGSERIAL PRIMARY KEY,
    window_end     TIMESTAMP NOT NULL,
    entity_a       VARCHAR(128) NOT NULL,
    entity_b       VARCHAR(128) NOT NULL,
    cooccur_count  INTEGER,
    baseline_count FLOAT,
    growth_rate    FLOAT,
    is_new_pair    BOOLEAN DEFAULT FALSE,
    UNIQUE(window_end, entity_a, entity_b)
);
CREATE INDEX idx_cooc_window ON entity_cooccurrence(window_end DESC, growth_rate DESC);

-- 话题簇（L4）
CREATE TABLE topic_clusters (
    id                     BIGSERIAL PRIMARY KEY,
    window_end             TIMESTAMP NOT NULL,
    cluster_label          VARCHAR(256),
    entities               TEXT[],
    msg_ids                BIGINT[],
    representative_msg_ids BIGINT[],
    size                   INTEGER,
    score                  FLOAT,
    created_at             TIMESTAMP DEFAULT NOW()
);
CREATE INDEX idx_tc_window ON topic_clusters(window_end DESC, score DESC);

-- 叙事简报（L5）
CREATE TABLE narrative_briefs (
    id               BIGSERIAL PRIMARY KEY,
    cluster_id       BIGINT REFERENCES topic_clusters(id),
    entity           VARCHAR(128),
    window_end       TIMESTAMP NOT NULL,
    narrative        TEXT,
    catalyst         TEXT,
    key_players      TEXT[],
    sentiment        VARCHAR(16),
    fund_logic       TEXT,
    risk             TEXT,
    stage            VARCHAR(16),
    confidence       FLOAT,
    time_sensitivity VARCHAR(8),
    raw_llm_output   JSONB,
    created_at       TIMESTAMP DEFAULT NOW()
);
```

### 8.3 Phase 3 新增：1 张 + Embedding列

```sql
-- 叙事生命周期
CREATE TABLE narrative_lifecycle (
    id            BIGSERIAL PRIMARY KEY,
    entity        VARCHAR(128) NOT NULL,
    first_seen    TIMESTAMP,
    peak_time     TIMESTAMP,
    peak_score    FLOAT,
    current_stage VARCHAR(16),
    stage_history JSONB,
    is_active     BOOLEAN DEFAULT TRUE,
    updated_at    TIMESTAMP DEFAULT NOW()
);

-- 为 normalized_messages 加pgvector
ALTER TABLE normalized_messages ADD COLUMN embedding VECTOR(1024);
```

---

## 9. 模块目录与接口

```
PomsAI/
├── db/
│   ├── connection.py              [保留]
│   ├── models.py                  [扩展：新表ORM]
│   └── repositories/              [保留+新增]
│
├── scheduler/
│   └── jobs.py                    [扩展：各层定时任务]
│
├── llm/
│   └── ollama_client.py           [保留，加batch封装]
│
├── prompts/
│   ├── entity_extract.txt         [新增：L1兜底]
│   ├── narrative_brief.txt        [新增：L5主力]
│   └── daily_report.txt           [新增：L5日报]
│
├── dictionaries/                  [新增]
│   ├── tickers.yaml
│   ├── chains.yaml
│   ├── narratives.yaml
│   ├── kols.yaml
│   └── spam_patterns.yaml
│
├── services/
│   ├── prefilter.py               [保留，小幅改造]
│   │
│   ├── l0_normalizer.py           [新增]
│   ├── l0_dedup.py                [新增：SimHash→Embedding两阶段]
│   │
│   ├── l1_entity_extractor.py     [新增]
│   ├── l1_sentiment.py            [新增]
│   │
│   ├── l2_sliding_counter.py      [新增：内存滑窗]
│   ├── l2_hotness.py              [新增：核心评分]
│   ├── l2_alert_trigger.py        [新增：实时激增]
│   │
│   ├── l3_cooccurrence.py         [新增]
│   ├── l3_lifecycle.py            [新增]
│   │
│   ├── l4_embedding.py            [新增：bge-m3封装]
│   ├── l4_clustering.py           [新增：HDBSCAN]
│   │
│   ├── l5_briefing.py             [新增，改造原level1_service]
│   ├── l5_daily_report.py         [新增，改造原level2_service]
│   │
│   └── l6_alerting.py             [新增：Telegram]
│
├── ui/
│   └── streamlit_app.py           [新增]
│
├── config/
│   └── settings.py                [扩展]
│
└── main.py                        [保留]
```

### 9.1 核心配置（config/settings.py 扩展）

```python
GROWTH_THRESHOLDS = {
    'alert_critical': 20.0,
    'alert_high':     10.0,
    'top_k_include':   5.0,
}

WINDOWS = {
    'short': 1,
    'baseline': 7,
    'smoothing': 2.0,
}

LLM_MODELS = {
    'entity_extract': 'qwen3:8b',
    'brief':          'qwen3:14b',
    'daily_report':   'qwen3:32b',
    'emergency':      'qwen3:8b',
}

TOP_K_FOR_BRIEF     = 20
REPRESENTATIVE_MSGS = 5
```

---

## 10. 调度时序

```
持续（流式）：
  L0+L1 处理新消息                        来一条处理一条 · <100ms/条
  L2 滑动窗口内存实时更新

每 15 分钟：
  L2 批量计算增长率 → hotness_snapshots   <30s
  L3 更新共现矩阵 → 检测新叙事            <10s
  检查激增警报触发

每 1 小时：
  L4 对 Top-K 热点跑 Embedding + HDBSCAN  2~5min
  L5 每簇生成 LLM 简报（Top 20）          10~30min（并行）
  L6 输出每小时简报Markdown

每日（凌晨）：
  L5 qwen3:32b 日度总评                   5~15min
  L6 生成每日雷达报告
  更新 narrative_lifecycle
  清理 30 天前原始数据（保留热点记录）

实时触发（任意时刻）：
  增长率 > 20x → 立即触发 L5 紧急简报 → L6 Telegram 警报 · <5s
  新词 ≥ 5 次  → 标记，纳入下次 L2 计算
```

---

## 11. 技术栈

| 类别 | 选型 | 理由 |
|------|------|------|
| 数据库 | PostgreSQL（初期SQLite可） | 成熟，后期可加pgvector |
| 时序加速（后期） | ClickHouse | 亿级时序毫秒级 |
| 向量库 | FAISS（本地文件） | **别一开始就上Milvus** |
| Embedding | bge-m3（sentence-transformers） | 中英混合最佳 |
| 近似去重 | datasketch（SimHash） | 毫秒级成熟库 |
| 分词 | jieba | 加密词汇可自定义 |
| 聚类 | HDBSCAN | 无需预设簇数 |
| LLM | Ollama（已有） | 复用 |
| 调度 | 现有scheduler | 复用 |
| 队列 | 内存队列（初期）/ Redis（后期） | 初期不引入新依赖 |
| 可视化 | Streamlit | Python原生快速搭建 |
| 警报 | Telegram Bot API | 简单可靠 |
| ORM | SQLAlchemy（已有） | 复用 |

---

## 12. 硬件资源规划

**主机**：96G内存，纯CPU

| 组件 | 内存 | 备注 |
|------|------|------|
| bge-m3 | ~2GB | 持续常驻，L0+L4共用 |
| qwen3:8b (Q4) | ~5GB | L1兜底+紧急简报 |
| qwen3:14b (Q4) | ~9GB | L5主力（常驻或按需） |
| qwen3:32b (Q4) | ~20GB | L5日报（每日1次，用后释放） |
| 滑窗计数器 | 1~3GB | 取决于实体数 |
| PostgreSQL | 2~4GB | DB缓存 |
| Python进程 | 1~2GB | |
| **合计日常** | **25~35GB** | 96G绰绰有余 |

**CPU推理参考**（qwen3:14b Q4）：
- 单次简报 ~500 token = 2~5min
- Top-20完整简报 = 40~100min（可并行）
- 建议：简报异步，不阻塞主流程

**优化**：
- bge-m3 + qwen3:8b 常驻，避免重复加载
- qwen3:32b 用完立即释放
- HDBSCAN计算与LLM推理错峰

---

## 13. 分阶段落地路径

> 方案4最重要的警告：**顺序错了，项目一定死。** 个人开发者必须按顺序做完一个Phase再进下一个。

### Phase 1：止血期（第 1 周）——立竿见影，零LLM

> **目标**：先把排行榜跑起来。没有LLM简报，你已经有可用产品。

| # | 任务 | 模块 | 产出 |
|---|------|------|------|
| 1.1 | 字段归一化 | l0_normalizer.py | 统一数据格式 |
| 1.2 | SimHash去重 | l0_dedup.py | LLM调用量砍30~50% |
| 1.3 | 词典 v1 | dictionaries/*.yaml | 覆盖主流币+你关注项目 |
| 1.4 | 正则+词典实体抽取 | l1_entity_extractor.py | 每条消息挂实体标签 |
| 1.5 | 增长率+排行 | l2_hotness.py + l2_sliding_counter.py | **每15min Top-20 排行榜** ✅ |
| 1.6 | 激增Telegram警报 | l2_alert_trigger.py + l6_alerting.py | 增长率>20x立即推送 |

**里程碑**：**零LLM**，能看到实时热度排行 + 激增实时推送。

**Phase 1 的意义**：验证"增长率策略是否真能发现Alpha"。这是整个产品最核心的假设验证。如果这一步跑出来你觉得"这排行榜没用"，那后面的LLM再漂亮也没意义。**先验证假设，再加复杂度。**

---

### Phase 2：升级期（第 2~4 周）——质量跃迁

| # | 任务 | 模块 | 产出 |
|---|------|------|------|
| 2.1 | 共现网络 | l3_cooccurrence.py | **新叙事检测** ⭐ |
| 2.2 | Embedding聚类 | l4_embedding.py + l4_clustering.py | 话题簇 |
| 2.3 | LLM定向简报 | l5_briefing.py | 每小时结构化简报 |
| 2.4 | 情绪分析 | l1_sentiment.py | 情绪维度 |
| 2.5 | LLM实体兜底 | l1_entity_extractor.py 增强 | 长尾覆盖 |

**里程碑**：系统能自动生成结构化热点简报 + 新叙事发现 + 激增警报。

---

### Phase 3：精细化（第 5~8 周）——打磨

| # | 任务 |
|---|------|
| 3.1 | 热点生命周期追踪（萌芽/爆发/高潮/衰退） |
| 3.2 | KOL传播路径分析（谁先提、怎么扩散） |
| 3.3 | 去重升级为 SimHash → bge-m3 两阶段 |
| 3.4 | Streamlit 面板（排行榜+共现图+生命周期） |
| 3.5 | qwen3:32b 日度总评 |
| 3.6 | 词典持续维护机制（新词自动提示） |

---

### Phase 4：高级演进（长期）

| 功能 | 说明 |
|------|------|
| 链上数据接入 | Dune Analytics API，链上行为与社交共振 |
| KOL影响力动态评分 | 历史喊单准确率更新权重 |
| 跨平台传播时序 | Twitter热→Discord爆→广场接盘 |
| 自动词典扩充 | 新词检测自动提示补充 |
| ClickHouse接入 | 数据量大后时序加速 |
| Telegram/Reddit接入 | 数据源扩展 |

---

## 14. 反模式清单

在设计和开发中，**以下做法明确禁止**：

| 反模式 | 为什么错 | 正确做法 |
|--------|---------|---------|
| 把全部原始消息丢给LLM总结 | Token爆炸，LLM平均化，看不到小而急的新热点 | 先统计发现，再LLM解释 |
| 只看绝对提及量 | BTC/ETH永远第一，看不到新叙事 | 看增长率，不看绝对值 |
| 用LLM做去重 | 浪费算力，100ms的事别花10分钟 | SimHash/Embedding做去重 |
| 单源判断热点 | 易被单平台噪声/水军误导 | 多源共振才是真信号 |
| 固定时间窗口触发 | 错过爆发期的前15分钟 | 实时激增检测+事件触发 |
| 把LLM当计数器 | LLM不擅长统计 | 统计交给代码，解释交给LLM |
| 一次把所有消息丢给LLM聚类 | 该Embedding+HDBSCAN做的事 | 向量聚类，LLM只看代表消息 |
| 忽略共现关系 | 只看单词频率看不到叙事形成 | 建立共现网络 |
| **一上来就做全套** | **个人开发者最大死法** | **严格按Phase 1→4推进** |
| **先上Milvus/微服务/ClickHouse** | 数据量不够，徒增复杂度 | SQLite/PG+FAISS就够 |

---

## 15. 给个人开发者的忠告（方案4精华）

这一节是**方案4**的核心贡献，是给你自己的警告。

### 15.1 你最大的敌人不是技术，是复杂度

复杂系统的死法：
- 调不通
- 跑不稳
- 调参地狱
- 永远在重构

**最后：你天天看架构图，却没有真正跑出Alpha。**

### 15.2 一上来想做全套 = 必死

正确节奏：
1. **先用纯规则跑Phase 1**。哪怕没有LLM、没有Embedding、没有聚类都不影响。
2. **先验证Alpha策略有没有效**。如果Top-20增长率榜本身就能帮你发现机会，那系统已经有价值。
3. **再决定要不要加LLM**。LLM是"解释器"，不是"发现器"。

### 15.3 LLM 是解释热点的工具，不是发现热点的工具

记住这句话：

> **LLM不是发现热点的工具。LLM是解释热点的工具。**

这是整个设计哲学。违反它，你会陷入token成本爆炸+推理质量下降的双重地狱。

### 15.4 加密领域的结构化优势

加密社交数据相比普通NLP有个天然优势：**结构化极强**。
- `$TICKER` 天然可正则
- 合约地址天然可正则
- `@handle` 天然可正则
- 链名、项目名、叙事词都有有限词汇表

**所以：第一阶段纯规则就足够。不要为了炫技上Embedding。**

Embedding在什么时候上？当你发现：
- 同义表达多（"大饼"=BTC）
- 英文变体多
- 语义重复（规则抓不到的重复）

**规则扛不住了，再上Embedding。** 不是反过来。

### 15.5 系统真正的灵魂

你系统真正的灵魂是一句话：

> **发现"新的连接"**
>
> 不是发现旧热点。
>
> 而是：原本无关的词，突然开始大量共现。

这是Phase 2共现网络的本质，也是**真正值钱的Alpha**。

### 15.6 本质上你在做什么

你这个系统本质上**不是**：
- ❌ AI总结工具
- ❌ 新闻聚合器

而**是**：
- ✅ **加密市场注意力流动探测器**

因为：

> **资金流向，本质上就是注意力流向。**
>
> **你如果能更早发现注意力迁移，这就是非常真实的信息优势。**

---

## 16. 后续演进

```
 现在             3个月             6个月              1年
  │                │                 │                 │
  ▼                ▼                 ▼                 ▼
多源采集        热点雷达           叙事追踪          Alpha系统
+统计排行  →  +LLM简报+警报  → +生命周期+KOL路径  → +链上共振+预测
```

**终极形态**：一个"加密世界信息压缩引擎"。

加密市场真正的alpha不是信息多，而是：**谁能更快从噪音中发现增长最快的叙事**。

做到Phase 3，就已经能持续稳定地在别人"感觉到热"之前若干小时，拿到结构化信号。这是真实的信息优势。

---

## 附录A：三份方案到 v3.0 的差异对照

| 维度 | 设计文档1 (v1) | 设计文档2 (v2) | 方案4 | **终极方案 v3.0** |
|---|---|---|---|---|
| 架构层数 | 6层 (L0~L5) | 7层 (L0~L6) | 5层极简 | **7层骨架 + MVP标注** |
| 共现网络 | 算法一节 | 独立L3层 | 第二优先级 | **独立L3，Phase 2核心** |
| 情绪分析 | 未强调 | 单独模块 | 未提 | **L1保留，Phase 2补** |
| 新词检测 | 未提 | 专门算法 | 强调实体增长 | **L2必做** |
| KOL权重 | 未强调 | author_weight | P3优先级 | **Phase 1埋点 + Phase 3增强** |
| 生命周期追踪 | 未提 | 独立机制 | 未提 | **Phase 3** |
| 分阶段节奏 | 4阶段较通用 | 4阶段完整 | **P0~P4强调先跑MVP** | **融合：MVP零LLM + 严格分阶段** |
| 开发者忠告 | 无 | 反模式清单 | **核心价值** | **专章 §15** |
| 过度设计警告 | 无 | 技术栈有提醒 | **反复强调** | **红线+反模式+忠告三层** |

---

*文档版本：v3.0 · 终极方案 · 三方案精华融合*
*硬件：96G内存 · 纯CPU*
*核心哲学：统计发现热点，LLM解释热点；先跑MVP验证Alpha，再加复杂度*
*最重要的一句话：**LLM是解释热点的工具，不是发现热点的工具。***
