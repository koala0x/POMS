# 需求文档：社交数据智能摘要后台服务 v1.0

## 1. 系统概述

一个常驻后台运行的 Python 服务，监听 PostgreSQL 数据库中两张原始数据表（Twitter、币安广场），每 30 秒轮询一次。当任意一张表中积累了 50 条未处理数据时，触发本地大模型（Qwen3:30b）进行一次摘要，结果存入一次摘要表。每小时整点对过去一小时的一次摘要结果再做二次摘要，结果存入二次摘要表。Twitter 和币安广场两个数据源全程独立处理，不合并。

---

## 2. 数据库表结构

### 2.1 原始数据表（已有，需增加字段）

**`twitter_posts`**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| content | TEXT | 推文正文 |
| author | VARCHAR | 作者 |
| posted_at | TIMESTAMP | 发帖时间 |
| created_at | TIMESTAMP | 入库时间 |
| is_summarized | BOOLEAN | 是否已被一次摘要处理，默认 FALSE |

**`binance_square_posts`**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| content | TEXT | 帖子正文 |
| author | VARCHAR | 作者 |
| posted_at | TIMESTAMP | 发帖时间 |
| created_at | TIMESTAMP | 入库时间 |
| is_summarized | BOOLEAN | 是否已被一次摘要处理，默认 FALSE |

### 2.2 一次摘要表（新建）

**`summary_level1`**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| source | VARCHAR | 数据来源：`twitter` / `binance_square` |
| summary | TEXT | 大模型生成的摘要内容 |
| raw_ids | BIGINT[] | 本次涉及的原始数据 ID 列表 |
| raw_count | INT | 本次摘要的原始条数（固定 50） |
| created_at | TIMESTAMP | 摘要生成时间 |
| is_summarized_l2 | BOOLEAN | 是否已被二次摘要处理，默认 FALSE |

### 2.3 二次摘要表（新建）

**`summary_level2`**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL PK | 主键 |
| source | VARCHAR | 数据来源：`twitter` / `binance_square` |
| summary | TEXT | 二次摘要内容 |
| level1_ids | BIGINT[] | 本次涉及的一次摘要 ID 列表 |
| level1_count | INT | 本次涉及的一次摘要条数 |
| period_start | TIMESTAMP | 覆盖的时间范围起点 |
| period_end | TIMESTAMP | 覆盖的时间范围终点 |
| created_at | TIMESTAMP | 生成时间 |

---

## 3. 核心业务逻辑

### 3.1 一次摘要流程（每 30 秒轮询）

```
每 30 秒执行一次，Twitter 和币安广场分别独立执行：

1. 查询 is_summarized = FALSE 的数据条数
2. 若 >= 50 条：
   a. 取出最早入库的 50 条（按 created_at ASC）
   b. 将这 50 条 content 拼接，发送给 Qwen3:30b 做一次摘要
   c. 将摘要结果写入 summary_level1
   d. 将这 50 条原始数据的 is_summarized 更新为 TRUE
   e. 记录日志
3. 若 < 50 条：跳过，等待下次轮询
```

### 3.2 二次摘要流程（每小时整点触发）

```
每小时整点执行一次，Twitter 和币安广场分别独立执行：

1. 查询过去 1 小时内 is_summarized_l2 = FALSE 的 summary_level1 记录
2. 若有数据：
   a. 将这些一次摘要的 summary 拼接，发送给 Qwen3:30b 做二次摘要
   b. 将二次摘要结果写入 summary_level2
   c. 将涉及的 summary_level1 记录的 is_summarized_l2 更新为 TRUE
   d. 记录日志
3. 若无数据：跳过，记录日志
```

---

## 4. 大模型接口

| 项目 | 内容 |
|------|------|
| 框架 | Ollama |
| 接口地址 | `http://localhost:11434/api/chat` |
| 模型名称 | `qwen3:30b` |
| 请求超时 | 120 秒（30B 模型推理较慢） |
| 失败重试 | 最多重试 3 次，每次间隔 10 秒 |

### Prompt 设计

**一次摘要 Prompt：**
```
你是一个加密货币市场信息分析师。
以下是来自 [Twitter/币安广场] 的 50 条最新帖子，
请提炼核心观点、市场情绪和热点话题，输出结构化摘要，控制在 500 字以内。

[帖子内容列表]
```

**二次摘要 Prompt：**
```
你是一个加密货币市场信息分析师。
以下是过去 1 小时内对 [Twitter/币安广场] 数据生成的若干条摘要，
请进一步归纳提炼，输出本小时最核心的市场趋势和关键信息，控制在 300 字以内。

[一次摘要列表]
```

---

## 5. 异常处理

| 场景 | 处理方式 |
|------|----------|
| 大模型接口超时 | 重试 3 次，每次间隔 10 秒，仍失败则跳过本批，记录错误日志，数据标记不更新（下次继续处理） |
| 大模型返回空内容 | 记录警告日志，本批数据标记不更新，下次重新处理 |
| 数据库连接中断 | 捕获异常，等待 5 秒后重连，服务不崩溃退出 |
| 一小时内无一次摘要数据 | 跳过二次摘要，记录日志 |
| 服务意外重启 | 依赖 `is_summarized` 标记保证幂等，重启后自动从未处理数据继续 |

---

## 6. 日志规范

| 项目 | 内容 |
|------|------|
| 日志文件路径 | `./logs/service.log` |
| 滚动策略 | 按天滚动，保留最近 30 天 |
| 日志级别 | INFO（正常流程） / WARNING（跳过场景） / ERROR（异常） |
| 关键日志节点 | 每次轮询结果、触发摘要、摘要成功写入、重试、失败 |

---

## 7. 项目目录结构

```
social-summary-service/
│
├── README.md                    # 项目说明文档
├── requirements.txt             # 依赖库清单
├── .env                         # 环境变量配置（不提交 git）
├── .env.example                 # 环境变量示例（提交 git）
├── .gitignore
│
├── config/
│   └── settings.py              # 读取 .env，统一暴露配置项
│
├── db/
│   ├── __init__.py
│   ├── connection.py            # 数据库连接池管理
│   ├── migrations/              # SQL 迁移脚本（手动执行）
│   │   ├── 001_add_is_summarized_to_twitter.sql
│   │   ├── 002_add_is_summarized_to_binance.sql
│   │   ├── 003_create_summary_level1.sql
│   │   └── 004_create_summary_level2.sql
│   └── repositories/
│       ├── __init__.py
│       ├── twitter_repo.py      # twitter_posts 表的查询与更新
│       ├── binance_repo.py      # binance_square_posts 表的查询与更新
│       ├── level1_repo.py       # summary_level1 表的读写
│       └── level2_repo.py       # summary_level2 表的读写
│
├── llm/
│   ├── __init__.py
│   └── ollama_client.py         # 封装 Ollama HTTP 调用、重试逻辑
│
├── prompts/
│   ├── level1_twitter.txt       # 一次摘要 Prompt 模板（Twitter）
│   ├── level1_binance.txt       # 一次摘要 Prompt 模板（币安广场）
│   ├── level2_twitter.txt       # 二次摘要 Prompt 模板（Twitter）
│   └── level2_binance.txt       # 二次摘要 Prompt 模板（币安广场）
│
├── services/
│   ├── __init__.py
│   ├── level1_service.py        # 一次摘要业务逻辑
│   └── level2_service.py        # 二次摘要业务逻辑
│
├── scheduler/
│   ├── __init__.py
│   └── jobs.py                  # APScheduler 任务注册与启动
│
├── logs/                        # 运行时日志目录（不提交 git）
│   └── .gitkeep
│
├── tests/
│   ├── __init__.py
│   ├── test_level1_service.py
│   ├── test_level2_service.py
│   └── test_ollama_client.py
│
└── main.py                      # 程序入口，启动 scheduler
```

---

## 8. 各层职责说明

| 层级 | 目录 | 职责 |
|------|------|------|
| 入口层 | `main.py` | 初始化配置、日志、启动调度器 |
| 调度层 | `scheduler/` | 注册定时任务，协调任务触发时机 |
| 业务层 | `services/` | 核心流程编排，不直接操作 DB 和 LLM |
| 数据层 | `db/repositories/` | 所有 SQL 操作，业务层只调接口 |
| 模型层 | `llm/` | 封装 Ollama 调用，对业务层屏蔽 HTTP 细节 |
| 配置层 | `config/` | 统一管理所有配置项，其他模块从这里读 |
| 提示词层 | `prompts/` | Prompt 与代码解耦，方便单独调整 |
| 迁移层 | `db/migrations/` | 数据库变更有版本记录，可追溯 |

---

## 9. 部署要求

| 项目 | 内容 |
|------|------|
| 运行方式 | 后台常驻进程（可用 `nohup` 或 `systemd` 管理） |
| Python 版本 | 3.10+ |
| 主要依赖库 | `psycopg2`、`requests`、`APScheduler`、`loguru` |
| 配置方式 | `.env` 文件，存放 DB 连接信息、Ollama 地址等，通过 `config/settings.py` 统一读取 |

---

完整文档就是这些，直接拿去用就行了。